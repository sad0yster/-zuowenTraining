# 素材模块优化实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 重构素材模块，提供分层递进的浏览体验、辩论式深度学习、切合作文的阅读材料

**Architecture:** 首页采用分层递进式布局（精选+入口），详情页增加辩论模式和阅读材料，使用Mintlify设计风格

**Tech Stack:** React 18, TypeScript, CSS (Mintlify风格), DeepSeek API, localStorage

---

## 文件结构

### 新增文件
- `src/components/MaterialsHome.tsx` — 素材首页（今日精选、最近学习、功能入口）
- `src/components/MaterialsHome.css` — 首页样式
- `src/components/DebateMode.tsx` — 辩论模式组件
- `src/components/DebateMode.css` — 辩论模式样式
- `src/components/ReadingMaterials.tsx` — 阅读材料组件
- `src/components/ReadingMaterials.css` — 阅读材料样式
- `src/components/LearningPaths.tsx` — 学习路径组件
- `src/components/LearningPaths.css` — 学习路径样式
- `src/components/ConceptMap.tsx` — 概念地图组件
- `src/components/ConceptMap.css` — 概念地图样式
- `src/data/readingMaterials.json` — 阅读材料数据
- `src/data/learningPaths.json` — 学习路径数据
- `src/services/knowledgeService.ts` — 知识库API服务
- `src/prompts/debate.ts` — 辩论模式prompt

### 修改文件
- `src/components/MaterialsPage.tsx` — 重构为路由入口
- `src/components/MaterialsPage.css` — 更新样式
- `src/components/MaterialDetail.tsx` — 增加辩论模式和阅读材料入口
- `src/components/MaterialDetail.css` — 更新样式
- `src/types/index.ts` — 增加新类型定义
- `src/services/storageService.ts` — 增加辩论记录存储
- `src/App.css` — 更新Mintlify设计变量

---

## Task 1: 更新设计变量（Mintlify风格）

**Files:**
- Modify: `src/App.css`

- [ ] **Step 1: 更新CSS变量为Mintlify风格**

```css
:root {
  /* ── Colors (Mintlify) ── */
  --color-primary: #0a0a0a;
  --color-secondary: #3a3a3c;
  --color-body: #5a5a5c;
  --color-muted: #767676;
  --color-disabled: #a8a8aa;
  --color-border: #e5e5e5;
  --color-border-dark: #ededed;
  --color-bg-subtle: #fafafa;
  --color-bg-hover: #f7f7f7;
  --color-bg-neutral: #f7f7f7;
  --color-bg-warm: #f0f0f0;
  --color-bg: #ffffff;
  --color-page: #f7f7f7;
  --color-warning: #d44;

  /* ── Accent ── */
  --color-accent: #00d4a4;
  --color-accent-deep: #00b48a;
  --color-accent-soft: rgba(0, 212, 164, 0.1);
  --color-tag: #00b48a;
  --color-tag-bg: rgba(0, 212, 164, 0.1);
  --color-surface-code: #1c1c1e;

  /* ── Radius ── */
  --radius-sm: 4px;
  --radius-md: 8px;
  --radius-lg: 12px;
  --radius-pill: 9999px;

  /* ── Shadow ── */
  --shadow-sm: rgba(0, 0, 0, 0.04) 0 1px 2px;
  --shadow-md: rgba(0, 0, 0, 0.08) 0 4px 12px;
  --shadow-accent: rgba(0, 212, 164, 0.08) 0 8px 24px;

  /* ── Transition ── */
  --transition-fast: 0.15s ease;
  --transition-base: 0.15s ease;

  /* ── Typography ── */
  --font-ui: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', 'PingFang SC', 'Hiragino Sans GB', 'Microsoft YaHei', sans-serif;
  --font-code: 'Geist Mono', 'SF Mono', Menlo, Consolas, monospace;
}
```

- [ ] **Step 2: 运行开发服务器验证样式更新**

Run: `npm run dev`
Expected: 页面正常加载，样式更新生效

- [ ] **Step 3: 提交**

```bash
git add src/App.css
git commit -m "style: update CSS variables to Mintlify design system"
```

---

## Task 2: 添加新类型定义

**Files:**
- Modify: `src/types/index.ts`

- [ ] **Step 1: 添加阅读材料类型**

```typescript
export type ReadingMaterialType = 'philosophy' | 'case' | 'history' | 'data';

export interface ReadingMaterial {
  id: string;
  materialId: string;
  type: ReadingMaterialType;
  title: string;
  content: string;
  source?: string;
  usageHint: string;
  quotable: boolean;
}
```

- [ ] **Step 2: 添加辩论记录类型**

```typescript
export type DebateMode = 'judge' | 'opponent';
export type DebateSide = 'for' | 'against';

export interface DebateRound {
  round: number;
  side: DebateSide;
  content: string;
  isUser: boolean;
  timestamp: number;
}

export interface DebateRecord {
  id: string;
  materialId: string;
  mode: DebateMode;
  rounds: DebateRound[];
  summary?: string;
  createdAt: number;
}
```

- [ ] **Step 3: 添加学习路径类型**

```typescript
export type PathType = 'theme' | 'ability';

export interface LearningPath {
  id: string;
  type: PathType;
  name: string;
  description: string;
  materialIds: string[];
  order: number;
}
```

- [ ] **Step 4: 提交**

```bash
git add src/types/index.ts
git commit -m "feat: add types for reading materials, debate, and learning paths"
```

---

## Task 3: 更新存储服务

**Files:**
- Modify: `src/services/storageService.ts`

- [ ] **Step 1: 添加辩论记录存储函数**

```typescript
const DEBATE_RECORDS_KEY = 'zuowen_debate_records';

export function saveDebateRecord(record: DebateRecord): void {
  const records = loadDebateRecords();
  const existingIndex = records.findIndex(r => r.id === record.id);
  if (existingIndex >= 0) {
    records[existingIndex] = record;
  } else {
    records.push(record);
  }
  localStorage.setItem(DEBATE_RECORDS_KEY, JSON.stringify(records));
}

export function loadDebateRecords(): DebateRecord[] {
  const data = localStorage.getItem(DEBATE_RECORDS_KEY);
  return data ? JSON.parse(data) : [];
}

export function loadDebateRecordsByMaterial(materialId: string): DebateRecord[] {
  return loadDebateRecords().filter(r => r.materialId === materialId);
}

export function clearDebateRecord(id: string): void {
  const records = loadDebateRecords().filter(r => r.id !== id);
  localStorage.setItem(DEBATE_RECORDS_KEY, JSON.stringify(records));
}
```

- [ ] **Step 2: 添加阅读材料缓存函数**

```typescript
const READING_CACHE_KEY = 'zuowen_reading_cache';

export function cacheReadingMaterials(materialId: string, materials: ReadingMaterial[]): void {
  const cache = loadReadingCache();
  cache[materialId] = { materials, cachedAt: Date.now() };
  localStorage.setItem(READING_CACHE_KEY, JSON.stringify(cache));
}

export function loadCachedReadingMaterials(materialId: string): ReadingMaterial[] | null {
  const cache = loadReadingCache();
  const entry = cache[materialId];
  if (!entry) return null;
  // Cache valid for 7 days
  if (Date.now() - entry.cachedAt > 7 * 24 * 60 * 60 * 1000) return null;
  return entry.materials;
}

function loadReadingCache(): Record<string, { materials: ReadingMaterial[]; cachedAt: number }> {
  const data = localStorage.getItem(READING_CACHE_KEY);
  return data ? JSON.parse(data) : {};
}
```

- [ ] **Step 3: 提交**

```bash
git add src/services/storageService.ts
git commit -m "feat: add storage for debate records and reading materials cache"
```

---

## Task 4: 创建素材首页组件

**Files:**
- Create: `src/components/MaterialsHome.tsx`
- Create: `src/components/MaterialsHome.css`

- [ ] **Step 1: 创建MaterialsHome组件**

```tsx
import { useState, useMemo } from 'react';
import type { Material } from '../types';
import materials from '../data/materials.json';
import './MaterialsHome.css';

const allMaterials = materials as Material[];

interface MaterialsHomeProps {
  onSelectMaterial: (material: Material) => void;
  onBrowseAll: () => void;
  onLearningPaths: () => void;
  onConceptMap: () => void;
}

export function MaterialsHome({ onSelectMaterial, onBrowseAll, onLearningPaths, onConceptMap }: MaterialsHomeProps) {
  // Get today's pick (rotate daily based on date)
  const todayIndex = new Date().getDate() % allMaterials.length;
  const todaysPick = allMaterials[todayIndex];

  // Get recent learning from localStorage
  const recentMaterials = useMemo(() => {
    const saved = localStorage.getItem('zuowen_material_discussions');
    if (!saved) return [];
    const discussions = JSON.parse(saved);
    return discussions
      .slice(-3)
      .reverse()
      .map((d: any) => allMaterials.find(m => m.id === d.materialId))
      .filter(Boolean);
  }, []);

  return (
    <div className="materials-home">
      <div className="mh-header">
        <h2>思辨素材</h2>
        <p className="mh-subtitle">用真实的情境激发真实的思考</p>
      </div>

      {/* Today's Pick */}
      <div className="mh-today" onClick={() => onSelectMaterial(todaysPick)}>
        <div className="mh-today-label">今日精选</div>
        <div className="mh-today-title">{todaysPick.title}</div>
        <div className="mh-today-tension">{todaysPick.coreTension}</div>
        <div className="mh-today-tags">
          {todaysPick.tags.map(tag => (
            <span key={tag} className="mh-tag">{tag}</span>
          ))}
        </div>
      </div>

      {/* Recent Learning */}
      {recentMaterials.length > 0 && (
        <div className="mh-recent">
          <div className="mh-section-label">最近学习</div>
          <div className="mh-recent-list">
            {recentMaterials.map((material: Material) => (
              <button
                key={material.id}
                className="mh-recent-item"
                onClick={() => onSelectMaterial(material)}
              >
                <span className="mh-recent-title">{material.title}</span>
                <span className="mh-recent-action">继续</span>
              </button>
            ))}
          </div>
        </div>
      )}

      {/* Entry Grid */}
      <div className="mh-grid">
        <button className="mh-entry" onClick={onBrowseAll}>
          <div className="mh-entry-title">全部素材</div>
          <div className="mh-entry-desc">18个素材 · 5个分类</div>
        </button>
        <button className="mh-entry" onClick={onLearningPaths}>
          <div className="mh-entry-title">学习路径</div>
          <div className="mh-entry-desc">系统化深入</div>
        </button>
        <button className="mh-entry" onClick={onConceptMap}>
          <div className="mh-entry-title">概念地图</div>
          <div className="mh-entry-desc">15个核心概念</div>
        </button>
        <button className="mh-entry" onClick={() => {
          const randomIndex = Math.floor(Math.random() * allMaterials.length);
          onSelectMaterial(allMaterials[randomIndex]);
        }}>
          <div className="mh-entry-title">随机探索</div>
          <div className="mh-entry-desc">换个角度看</div>
        </button>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: 创建MaterialsHome样式**

```css
.materials-home {
  padding: 0;
}

.mh-header {
  margin-bottom: 32px;
}

.mh-header h2 {
  font-size: 28px;
  font-weight: 600;
  letter-spacing: -0.5px;
  color: var(--color-primary);
  margin-bottom: 4px;
}

.mh-subtitle {
  font-size: 13px;
  color: var(--color-body);
}

/* Today's Pick */
.mh-today {
  background: var(--color-bg-neutral);
  border-radius: var(--radius-lg);
  padding: 24px;
  margin-bottom: 32px;
  cursor: pointer;
  transition: background var(--transition-fast);
}

.mh-today:hover {
  background: var(--color-border-dark);
}

.mh-today-label {
  font-size: 11px;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  color: var(--color-body);
  margin-bottom: 12px;
}

.mh-today-title {
  font-size: 22px;
  font-weight: 600;
  color: var(--color-primary);
  margin-bottom: 8px;
  letter-spacing: -0.3px;
}

.mh-today-tension {
  font-size: 14px;
  color: var(--color-body);
  line-height: 1.50;
  margin-bottom: 16px;
}

.mh-today-tags {
  display: flex;
  gap: 8px;
  flex-wrap: wrap;
}

.mh-tag {
  font-size: 13px;
  font-weight: 500;
  padding: 2px 8px;
  border-radius: var(--radius-sm);
  background: var(--color-tag-bg);
  color: var(--color-tag);
}

/* Recent Learning */
.mh-recent {
  margin-bottom: 32px;
}

.mh-section-label {
  font-size: 11px;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  color: var(--color-body);
  margin-bottom: 12px;
}

.mh-recent-list {
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  overflow: hidden;
}

.mh-recent-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 16px 20px;
  border: none;
  border-bottom: 1px solid var(--color-border-dark);
  background: var(--color-bg);
  width: 100%;
  cursor: pointer;
  font-family: inherit;
  transition: background var(--transition-fast);
}

.mh-recent-item:last-child {
  border-bottom: none;
}

.mh-recent-item:hover {
  background: var(--color-bg-hover);
}

.mh-recent-title {
  font-size: 14px;
  font-weight: 500;
  color: var(--color-primary);
}

.mh-recent-action {
  font-size: 14px;
  font-weight: 500;
  color: var(--color-accent);
}

/* Entry Grid */
.mh-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 12px;
}

.mh-entry {
  padding: 20px;
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  background: var(--color-bg);
  text-align: left;
  cursor: pointer;
  font-family: inherit;
  transition: border-color var(--transition-fast);
}

.mh-entry:hover {
  border-color: var(--color-accent);
}

.mh-entry-title {
  font-size: 14px;
  font-weight: 600;
  color: var(--color-primary);
  margin-bottom: 4px;
}

.mh-entry-desc {
  font-size: 13px;
  color: var(--color-body);
}
```

- [ ] **Step 3: 运行开发服务器验证**

Run: `npm run dev`
Expected: 首页组件正常渲染，样式符合Mintlify风格

- [ ] **Step 4: 提交**

```bash
git add src/components/MaterialsHome.tsx src/components/MaterialsHome.css
git commit -m "feat: add MaterialsHome component with Mintlify design"
```

---

## Task 5: 创建辩论模式组件

**Files:**
- Create: `src/components/DebateMode.tsx`
- Create: `src/components/DebateMode.css`
- Create: `src/prompts/debate.ts`

- [ ] **Step 1: 创建辩论prompt**

```typescript
export const DEBATE_PROMPTS = {
  // AI当对手时，生成对立论点
  generateOpponent: (material: { title: string; situation: string; coreTension: string }, userSide: 'for' | 'against', userArgument: string, round: number) => `
你是一个辩论对手。用户选择了${userSide === 'for' ? '正方' : '反方'}立场。

素材：${material.title}
情境：${material.situation}
核心张力：${material.coreTension}

用户第${round}轮论点：
${userArgument}

请写一段${userSide === 'for' ? '反方' : '正方'}的反驳论点。要求：
1. 直接回应用户的论点
2. 提出新的视角或证据
3. 逻辑清晰，有说服力
4. 长度150-200字
`,

  // AI当裁判时，点评双方论点
  judgeEvaluation: (material: { title: string; coreTension: string }, forArgument: string, againstArgument: string) => `
你是辩论裁判。请评价以下双方论点。

素材：${material.title}
核心张力：${material.coreTension}

正方论点：
${forArgument}

反方论点：
${againstArgument}

请从以下角度评价：
1. 逻辑强度（是否有漏洞）
2. 证据使用（是否有说服力）
3. 视角独特性（是否有新意）

最后给出综合判断：哪方更有说服力，为什么？
`,

  // 辩论总结
  debateSummary: (material: { title: string }, rounds: { side: string; content: string }[]) => `
请总结以下辩论。

素材：${material.title}

辩论记录：
${rounds.map(r => `${r.side === 'for' ? '正方' : '反方'}：${r.content}`).join('\n\n')}

请给出：
1. 双方的核心分歧点
2. 各自的强项和弱项
3. 这个辩论揭示了什么深层思考
4. 作文中可以如何使用这个辩证思考
`
};
```

- [ ] **Step 2: 创建DebateMode组件**

```tsx
import { useState, useRef, useEffect } from 'react';
import type { Material, DebateMode as DebateModeType, DebateSide, DebateRound } from '../types';
import { generateId } from '../services/aiService';
import { sendDebateMessage } from '../services/aiService';
import { DEBATE_PROMPTS } from '../prompts/debate';
import { saveDebateRecord } from '../services/storageService';
import { ChatBubble } from './ChatBubble';
import { LoadingDots } from './LoadingDots';
import './DebateMode.css';

interface DebateModeProps {
  material: Material;
  onBack: () => void;
}

export function DebateMode({ material, onBack }: DebateModeProps) {
  const [mode, setMode] = useState<DebateModeType | null>(null);
  const [userSide, setUserSide] = useState<DebateSide | null>(null);
  const [rounds, setRounds] = useState<DebateRound[]>([]);
  const [currentRound, setCurrentRound] = useState(1);
  const [input, setInput] = useState('');
  const [loading, setLoading] = useState(false);
  const [summary, setSummary] = useState<string | null>(null);
  const bottomRef = useRef<HTMLDivElement>(null);

  useEffect(() => {
    bottomRef.current?.scrollIntoView({ behavior: 'smooth' });
  }, [rounds]);

  const handleStartDebate = (selectedMode: DebateModeType) => {
    setMode(selectedMode);
    if (selectedMode === 'opponent') {
      // In opponent mode, user needs to choose side first
    } else {
      // In judge mode, start with for side
      setRounds([]);
      setCurrentRound(1);
    }
  };

  const handleSelectSide = (side: DebateSide) => {
    setUserSide(side);
    setRounds([]);
    setCurrentRound(1);
  };

  const handleSend = async () => {
    const trimmed = input.trim();
    if (!trimmed || loading) return;

    const userRound: DebateRound = {
      round: currentRound,
      side: mode === 'opponent' ? userSide! : (currentRound % 2 === 1 ? 'for' : 'against'),
      content: trimmed,
      isUser: true,
      timestamp: Date.now(),
    };

    setRounds(prev => [...prev, userRound]);
    setInput('');
    setLoading(true);

    try {
      if (mode === 'opponent') {
        // AI generates opponent argument
        const prompt = DEBATE_PROMPTS.generateOpponent(material, userSide!, trimmed, currentRound);
        const response = await sendDebateMessage(prompt);
        
        const aiRound: DebateRound = {
          round: currentRound,
          side: userSide === 'for' ? 'against' : 'for',
          content: response,
          isUser: false,
          timestamp: Date.now(),
        };
        setRounds(prev => [...prev, aiRound]);
        setCurrentRound(prev => prev + 1);
      } else {
        // Judge mode - after both sides presented, AI evaluates
        if (currentRound >= 2) {
          const forArg = rounds.find(r => r.side === 'for')?.content || '';
          const againstArg = rounds.find(r => r.side === 'against')?.content || '';
          const prompt = DEBATE_PROMPTS.judgeEvaluation(material, forArg, againstArg);
          const response = await sendDebateMessage(prompt);
          setSummary(response);
        } else {
          setCurrentRound(prev => prev + 1);
        }
      }
    } catch {
      setRounds(prev => [...prev, {
        round: currentRound,
        side: userSide || 'for',
        content: '抱歉，出了点问题。请稍后再试。',
        isUser: false,
        timestamp: Date.now(),
      }]);
    } finally {
      setLoading(false);
    }
  };

  const handleKeyDown = (e: React.KeyboardEvent) => {
    if (e.key === 'Enter' && !e.shiftKey) {
      e.preventDefault();
      handleSend();
    }
  };

  const handleSaveDebate = () => {
    const record = {
      id: generateId(),
      materialId: material.id,
      mode: mode!,
      rounds,
      summary: summary || undefined,
      createdAt: Date.now(),
    };
    saveDebateRecord(record);
    onBack();
  };

  // Mode selection screen
  if (!mode) {
    return (
      <div className="debate-mode">
        <div className="dm-header">
          <button className="dm-back" onClick={onBack}>←</button>
          <h3>{material.title}</h3>
        </div>
        <div className="dm-situation">{material.situation}</div>
        <div className="dm-tension">
          <div className="dm-tension-label">核心张力</div>
          <div className="dm-tension-value">{material.coreTension}</div>
        </div>
        <div className="dm-mode-select">
          <div className="dm-mode-label">选择模式</div>
          <div className="dm-mode-buttons">
            <button className="dm-mode-btn" onClick={() => handleStartDebate('judge')}>
              用户当裁判
            </button>
            <button className="dm-mode-btn dm-mode-btn-active" onClick={() => handleStartDebate('opponent')}>
              AI当对手
            </button>
          </div>
        </div>
      </div>
    );
  }

  // Side selection screen (opponent mode only)
  if (mode === 'opponent' && !userSide) {
    return (
      <div className="debate-mode">
        <div className="dm-header">
          <button className="dm-back" onClick={() => setMode(null)}>←</button>
          <h3>选择立场</h3>
        </div>
        <div className="dm-side-select">
          <button className="dm-side-btn" onClick={() => handleSelectSide('for')}>
            正方：应该扳动道岔
          </button>
          <button className="dm-side-btn" onClick={() => handleSelectSide('against')}>
            反方：不应该扳动道岔
          </button>
        </div>
      </div>
    );
  }

  // Debate screen
  return (
    <div className="debate-mode">
      <div className="dm-header">
        <button className="dm-back" onClick={onBack}>←</button>
        <h3>辩论：{material.title}</h3>
      </div>

      <div className="dm-debate-area">
        {/* Round indicator */}
        <div className="dm-round-indicator">
          <div className="dm-sides">
            <span className={`dm-side ${rounds.length % 2 === 0 ? 'dm-side-active' : ''}`}>
              正方
            </span>
            <span className={`dm-side ${rounds.length % 2 === 1 ? 'dm-side-active' : ''}`}>
              反方
            </span>
          </div>
          <span className="dm-round-number">第 {currentRound} 轮</span>
        </div>

        {/* Rounds display */}
        <div className="dm-rounds">
          {rounds.map((round, index) => (
            <div key={index} className={`dm-round ${round.isUser ? 'dm-round-user' : 'dm-round-ai'}`}>
              <div className="dm-round-header">
                <span className={`dm-round-side ${round.side === 'for' ? 'dm-round-side-for' : 'dm-round-side-against'}`}>
                  {round.side === 'for' ? '正方' : '反方'}
                </span>
                <span className="dm-round-role">{round.isUser ? '我' : 'AI'}</span>
              </div>
              <div className="dm-round-content">{round.content}</div>
            </div>
          ))}
          {loading && <LoadingDots />}
          <div ref={bottomRef} />
        </div>

        {/* Summary */}
        {summary && (
          <div className="dm-summary">
            <div className="dm-summary-label">综合评估</div>
            <div className="dm-summary-content">{summary}</div>
            <button className="dm-save-btn" onClick={handleSaveDebate}>
              保存并返回
            </button>
          </div>
        )}

        {/* Input */}
        {!summary && (
          <div className="dm-input-area">
            <textarea
              className="dm-input"
              value={input}
              onChange={(e) => setInput(e.target.value)}
              onKeyDown={handleKeyDown}
              placeholder={mode === 'opponent' 
                ? `写下你的${userSide === 'for' ? '正方' : '反方'}论点...`
                : `写下${currentRound % 2 === 1 ? '正方' : '反方'}论点...`
              }
              disabled={loading}
            />
            <button
              className="dm-send-btn"
              onClick={handleSend}
              disabled={loading || !input.trim()}
            >
              提交
            </button>
          </div>
        )}
      </div>
    </div>
  );
}
```

- [ ] **Step 3: 创建DebateMode样式**

```css
.debate-mode {
  padding: 0;
}

.dm-header {
  display: flex;
  align-items: center;
  gap: 12px;
  margin-bottom: 24px;
}

.dm-back {
  font-size: 14px;
  color: var(--color-body);
  background: none;
  border: none;
  cursor: pointer;
  padding: 4px 8px;
}

.dm-back:hover {
  color: var(--color-primary);
}

.dm-header h3 {
  font-size: 18px;
  font-weight: 600;
  color: var(--color-primary);
}

.dm-situation {
  font-size: 14px;
  line-height: 1.70;
  color: var(--color-secondary);
  margin-bottom: 24px;
}

.dm-tension {
  background: var(--color-bg-neutral);
  border-radius: var(--radius-md);
  padding: 16px;
  margin-bottom: 24px;
}

.dm-tension-label {
  font-size: 11px;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  color: var(--color-body);
  margin-bottom: 8px;
}

.dm-tension-value {
  font-size: 16px;
  font-weight: 500;
  color: var(--color-primary);
}

/* Mode Selection */
.dm-mode-select {
  margin-bottom: 24px;
}

.dm-mode-label {
  font-size: 11px;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  color: var(--color-body);
  margin-bottom: 12px;
}

.dm-mode-buttons {
  display: flex;
  gap: 8px;
}

.dm-mode-btn {
  flex: 1;
  padding: 10px 20px;
  border: 1px solid var(--color-border);
  border-radius: var(--radius-pill);
  background: transparent;
  font-size: 14px;
  font-weight: 500;
  color: var(--color-body);
  cursor: pointer;
  font-family: inherit;
}

.dm-mode-btn:hover {
  border-color: var(--color-primary);
  color: var(--color-primary);
}

.dm-mode-btn-active {
  background: var(--color-primary);
  border-color: var(--color-primary);
  color: var(--color-bg);
}

/* Side Selection */
.dm-side-select {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.dm-side-btn {
  padding: 16px 20px;
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  background: var(--color-bg);
  font-size: 14px;
  font-weight: 500;
  color: var(--color-primary);
  cursor: pointer;
  font-family: inherit;
  text-align: left;
}

.dm-side-btn:hover {
  border-color: var(--color-accent);
}

/* Debate Area */
.dm-debate-area {
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  overflow: hidden;
}

.dm-round-indicator {
  padding: 12px 20px;
  border-bottom: 1px solid var(--color-border-dark);
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.dm-sides {
  display: flex;
  gap: 8px;
}

.dm-side {
  font-size: 12px;
  font-weight: 500;
  padding: 4px 12px;
  border-radius: var(--radius-pill);
  background: var(--color-bg-neutral);
  color: var(--color-body);
}

.dm-side-active {
  background: var(--color-accent-soft);
  color: var(--color-accent-deep);
  font-weight: 600;
}

.dm-round-number {
  font-size: 12px;
  color: var(--color-body);
}

/* Rounds */
.dm-rounds {
  padding: 20px;
  max-height: 400px;
  overflow-y: auto;
}

.dm-round {
  margin-bottom: 16px;
  padding: 12px 16px;
  border-radius: var(--radius-md);
  background: var(--color-bg-neutral);
}

.dm-round-user {
  border-left: 3px solid var(--color-accent);
}

.dm-round-ai {
  border-left: 3px solid var(--color-border);
}

.dm-round-header {
  display: flex;
  justify-content: space-between;
  margin-bottom: 8px;
}

.dm-round-side {
  font-size: 12px;
  font-weight: 600;
  padding: 2px 8px;
  border-radius: var(--radius-sm);
}

.dm-round-side-for {
  background: var(--color-accent-soft);
  color: var(--color-accent-deep);
}

.dm-round-side-against {
  background: var(--color-bg-neutral);
  color: var(--color-body);
}

.dm-round-role {
  font-size: 12px;
  color: var(--color-body);
}

.dm-round-content {
  font-size: 14px;
  line-height: 1.60;
  color: var(--color-secondary);
}

/* Summary */
.dm-summary {
  padding: 20px;
  border-top: 1px solid var(--color-border-dark);
  background: var(--color-bg-subtle);
}

.dm-summary-label {
  font-size: 11px;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  color: var(--color-body);
  margin-bottom: 12px;
}

.dm-summary-content {
  font-size: 14px;
  line-height: 1.60;
  color: var(--color-secondary);
  margin-bottom: 16px;
}

.dm-save-btn {
  width: 100%;
  padding: 10px 20px;
  border: none;
  border-radius: var(--radius-pill);
  background: var(--color-primary);
  font-size: 14px;
  font-weight: 500;
  color: var(--color-bg);
  cursor: pointer;
  font-family: inherit;
}

.dm-save-btn:hover {
  background: var(--color-accent-deep);
}

/* Input */
.dm-input-area {
  padding: 16px 20px;
  border-top: 1px solid var(--color-border-dark);
}

.dm-input {
  width: 100%;
  padding: 12px;
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  font-size: 14px;
  line-height: 1.50;
  resize: none;
  height: 100px;
  font-family: inherit;
  color: var(--color-primary);
  margin-bottom: 12px;
}

.dm-input:focus {
  outline: none;
  border-color: var(--color-accent);
}

.dm-send-btn {
  float: right;
  padding: 10px 20px;
  border: none;
  border-radius: var(--radius-pill);
  background: var(--color-primary);
  font-size: 14px;
  font-weight: 500;
  color: var(--color-bg);
  cursor: pointer;
  font-family: inherit;
}

.dm-send-btn:disabled {
  background: var(--color-border);
  color: var(--color-muted);
  cursor: not-allowed;
}

.dm-send-btn:hover:not(:disabled) {
  background: var(--color-accent-deep);
}
```

- [ ] **Step 4: 运行开发服务器验证**

Run: `npm run dev`
Expected: 辩论模式组件正常渲染，样式符合Mintlify风格

- [ ] **Step 5: 提交**

```bash
git add src/components/DebateMode.tsx src/components/DebateMode.css src/prompts/debate.ts
git commit -m "feat: add DebateMode component with judge and opponent modes"
```

---

## Task 6: 创建阅读材料组件

**Files:**
- Create: `src/components/ReadingMaterials.tsx`
- Create: `src/components/ReadingMaterials.css`
- Create: `src/data/readingMaterials.json`

- [ ] **Step 1: 创建阅读材料数据**

```json
[
  {
    "id": "rm1",
    "materialId": "m1",
    "type": "philosophy",
    "title": "康德：人是目的，不是手段",
    "content": "康德认为，每个人都具有内在价值，不能仅仅被当作实现某种目的的工具。在电车难题中，扳动道岔就是把那一个人当成了拯救五个人的手段。这种"工具化"的思维方式违背了康德的道德律令。",
    "source": "《道德形而上学基础》",
    "usageHint": "可用于论证"人的价值"、"道德底线"、"功利主义批判"等主题",
    "quotable": true
  },
  {
    "id": "rm2",
    "materialId": "m1",
    "type": "case",
    "title": "自动驾驶的伦理困境",
    "content": "2018年，Uber自动驾驶汽车撞死行人事件引发全球讨论：当机器必须做出生死抉择时，应该遵循什么原则？这暴露了电车难题在现实中的应用困境。",
    "source": "新闻报道",
    "usageHint": "可用于论证"科技伦理"、"人工智能与人类决策"等主题",
    "quotable": true
  }
]
```

- [ ] **Step 2: 创建ReadingMaterials组件**

```tsx
import { useState } from 'react';
import type { ReadingMaterial } from '../types';
import readingMaterialsData from '../data/readingMaterials.json';
import './ReadingMaterials.css';

const allReadingMaterials = readingMaterialsData as ReadingMaterial[];

interface ReadingMaterialsProps {
  materialId: string;
}

export function ReadingMaterials({ materialId }: ReadingMaterialsProps) {
  const [expandedId, setExpandedId] = useState<string | null>(null);
  
  const materials = allReadingMaterials.filter(rm => rm.materialId === materialId);

  if (materials.length === 0) return null;

  const typeLabels: Record<string, string> = {
    philosophy: '哲学观点',
    case: '现实案例',
    history: '历史典故',
    data: '数据事实',
  };

  return (
    <div className="reading-materials">
      <div className="rm-label">延伸阅读</div>
      <div className="rm-list">
        {materials.map(rm => (
          <div key={rm.id} className="rm-card">
            <div className="rm-tags">
              <span className="rm-type-tag">{typeLabels[rm.type]}</span>
              {rm.quotable && <span className="rm-quotable-tag">可直接引用</span>}
            </div>
            <div className="rm-title">{rm.title}</div>
            <div className="rm-content">
              {expandedId === rm.id ? rm.content : rm.content.slice(0, 100) + '...'}
            </div>
            {rm.content.length > 100 && (
              <button 
                className="rm-expand-btn"
                onClick={() => setExpandedId(expandedId === rm.id ? null : rm.id)}
              >
                {expandedId === rm.id ? '收起' : '展开全文'}
              </button>
            )}
            {rm.source && <div className="rm-source">来源：{rm.source}</div>}
          </div>
        ))}
      </div>
      <div className="rm-usage-hint">
        <div className="rm-usage-label">作文素材提示</div>
        <div className="rm-usage-text">
          以上材料可直接用于{materials[0]?.usageHint?.split('可用于')[1] || '相关主题'}的作文论证
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 3: 创建ReadingMaterials样式**

```css
.reading-materials {
  margin-top: 24px;
}

.rm-label {
  font-size: 11px;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  color: var(--color-body);
  margin-bottom: 12px;
}

.rm-list {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.rm-card {
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  padding: 20px;
}

.rm-tags {
  display: flex;
  gap: 8px;
  margin-bottom: 12px;
}

.rm-type-tag {
  font-size: 11px;
  font-weight: 600;
  padding: 2px 8px;
  border-radius: var(--radius-sm);
  background: var(--color-tag-bg);
  color: var(--color-tag);
}

.rm-quotable-tag {
  font-size: 11px;
  font-weight: 600;
  padding: 2px 8px;
  border-radius: var(--radius-sm);
  background: var(--color-bg-neutral);
  color: var(--color-body);
}

.rm-title {
  font-size: 16px;
  font-weight: 600;
  color: var(--color-primary);
  margin-bottom: 8px;
  letter-spacing: -0.3px;
}

.rm-content {
  font-size: 14px;
  line-height: 1.70;
  color: var(--color-secondary);
  margin-bottom: 12px;
}

.rm-expand-btn {
  font-size: 13px;
  font-weight: 500;
  color: var(--color-accent-deep);
  background: none;
  border: none;
  cursor: pointer;
  padding: 0;
  font-family: inherit;
}

.rm-expand-btn:hover {
  text-decoration: underline;
}

.rm-source {
  font-size: 12px;
  color: var(--color-muted);
  margin-top: 8px;
}

.rm-usage-hint {
  background: var(--color-bg-neutral);
  border-radius: var(--radius-md);
  padding: 16px;
  margin-top: 16px;
}

.rm-usage-label {
  font-size: 11px;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  color: var(--color-body);
  margin-bottom: 8px;
}

.rm-usage-text {
  font-size: 14px;
  line-height: 1.60;
  color: var(--color-secondary);
}
```

- [ ] **Step 4: 运行开发服务器验证**

Run: `npm run dev`
Expected: 阅读材料组件正常渲染，样式符合Mintlify风格

- [ ] **Step 5: 提交**

```bash
git add src/components/ReadingMaterials.tsx src/components/ReadingMaterials.css src/data/readingMaterials.json
git commit -m "feat: add ReadingMaterials component with sample data"
```

---

## Task 7: 更新素材详情页

**Files:**
- Modify: `src/components/MaterialDetail.tsx`
- Modify: `src/components/MaterialDetail.css`

- [ ] **Step 1: 添加辩论模式和阅读材料入口**

在MaterialDetail.tsx中，在现有对话区域之后添加：

```tsx
{/* Mode Selection */}
<div className="md-mode-select">
  <div className="md-mode-label">选择模式</div>
  <div className="md-mode-buttons">
    <button 
      className={`md-mode-btn ${!debateMode ? 'md-mode-btn-active' : ''}`}
      onClick={() => setDebateMode(false)}
    >
      自由讨论
    </button>
    <button 
      className={`md-mode-btn ${debateMode ? 'md-mode-btn-active' : ''}`}
      onClick={() => setDebateMode(true)}
    >
      辩论模式
    </button>
  </div>
</div>

{/* Conditional render based on mode */}
{debateMode ? (
  <DebateMode material={material} onBack={() => setDebateMode(false)} />
) : (
  /* Existing chat area */
  <div className="md-chat-area">
    {/* ... existing chat code ... */}
  </div>
)}

{/* Reading Materials */}
<ReadingMaterials materialId={material.id} />
```

- [ ] **Step 2: 添加状态和导入**

```tsx
import { DebateMode } from './DebateMode';
import { ReadingMaterials } from './ReadingMaterials';

// In component:
const [debateMode, setDebateMode] = useState(false);
```

- [ ] **Step 3: 更新详情页样式**

```css
.md-mode-select {
  margin-bottom: 24px;
}

.md-mode-label {
  font-size: 11px;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  color: var(--color-body);
  margin-bottom: 12px;
}

.md-mode-buttons {
  display: flex;
  gap: 8px;
}

.md-mode-btn {
  flex: 1;
  padding: 10px 20px;
  border: 1px solid var(--color-border);
  border-radius: var(--radius-pill);
  background: transparent;
  font-size: 14px;
  font-weight: 500;
  color: var(--color-body);
  cursor: pointer;
  font-family: inherit;
}

.md-mode-btn:hover {
  border-color: var(--color-primary);
  color: var(--color-primary);
}

.md-mode-btn-active {
  background: var(--color-primary);
  border-color: var(--color-primary);
  color: var(--color-bg);
}
```

- [ ] **Step 4: 运行开发服务器验证**

Run: `npm run dev`
Expected: 详情页显示模式选择，可以切换自由讨论和辩论模式

- [ ] **Step 5: 提交**

```bash
git add src/components/MaterialDetail.tsx src/components/MaterialDetail.css
git commit -m "feat: add debate mode and reading materials to MaterialDetail"
```

---

## Task 8: 更新素材主页路由

**Files:**
- Modify: `src/components/MaterialsPage.tsx`
- Modify: `src/components/MaterialsPage.css`

- [ ] **Step 1: 重构MaterialsPage为路由入口**

```tsx
import { useState } from 'react';
import type { Material } from '../types';
import { MaterialsHome } from './MaterialsHome';
import { MaterialDetail } from './MaterialDetail';
import './MaterialsPage.css';

type MaterialsView = 'home' | 'detail' | 'browse' | 'paths' | 'concept-map';

export function MaterialsPage() {
  const [view, setView] = useState<MaterialsView>('home');
  const [selectedMaterial, setSelectedMaterial] = useState<Material | null>(null);

  const handleSelectMaterial = (material: Material) => {
    setSelectedMaterial(material);
    setView('detail');
  };

  const handleBack = () => {
    setSelectedMaterial(null);
    setView('home');
  };

  if (view === 'detail' && selectedMaterial) {
    return (
      <MaterialDetail
        material={selectedMaterial}
        onBack={handleBack}
      />
    );
  }

  // For now, other views just show home
  // In future tasks, we'll implement browse, paths, and concept-map views
  return (
    <MaterialsHome
      onSelectMaterial={handleSelectMaterial}
      onBrowseAll={() => setView('browse')}
      onLearningPaths={() => setView('paths')}
      onConceptMap={() => setView('concept-map')}
    />
  );
}
```

- [ ] **Step 2: 更新样式**

```css
.materials-page {
  padding: 0;
}
```

- [ ] **Step 3: 运行开发服务器验证**

Run: `npm run dev`
Expected: 素材模块显示新首页，点击素材进入详情页

- [ ] **Step 4: 提交**

```bash
git add src/components/MaterialsPage.tsx src/components/MaterialsPage.css
git commit -m "refactor: MaterialsPage as routing entry with new home"
```

---

## Task 9: 创建全部素材浏览页

**Files:**
- Create: `src/components/MaterialsBrowse.tsx`
- Create: `src/components/MaterialsBrowse.css`

- [ ] **Step 1: 创建MaterialsBrowse组件**

```tsx
import { useState, useMemo } from 'react';
import type { Material, MaterialCategory } from '../types';
import materials from '../data/materials.json';
import './MaterialsBrowse.css';

const allMaterials = materials as Material[];

const CATEGORIES: { key: MaterialCategory; label: string }[] = [
  { key: 'self-and-self', label: '我与自我' },
  { key: 'self-and-others', label: '我与他人' },
  { key: 'self-and-world', label: '我与世界' },
  { key: 'self-and-era', label: '我与时代' },
  { key: 'self-and-tradition', label: '我与传统' },
];

interface MaterialsBrowseProps {
  onSelectMaterial: (material: Material) => void;
  onBack: () => void;
}

export function MaterialsBrowse({ onSelectMaterial, onBack }: MaterialsBrowseProps) {
  const [activeCategory, setActiveCategory] = useState<MaterialCategory | 'all'>('all');
  const [search, setSearch] = useState('');

  const filtered = useMemo(() => {
    let list = allMaterials;
    if (activeCategory !== 'all') {
      list = list.filter((m) => m.category === activeCategory);
    }
    if (search.trim()) {
      const kw = search.trim().toLowerCase();
      list = list.filter(
        (m) =>
          m.title.toLowerCase().includes(kw) ||
          m.coreTension.toLowerCase().includes(kw) ||
          m.tags.some((t) => t.toLowerCase().includes(kw)) ||
          m.situation.toLowerCase().includes(kw)
      );
    }
    return list;
  }, [activeCategory, search]);

  return (
    <div className="materials-browse">
      <div className="mb-header">
        <button className="mb-back" onClick={onBack}>←</button>
        <h2>全部素材</h2>
      </div>

      <input
        className="mb-search"
        type="text"
        placeholder="搜索素材关键词…"
        value={search}
        onChange={(e) => setSearch(e.target.value)}
      />

      <div className="mb-categories">
        <button
          className={`mb-category ${activeCategory === 'all' ? 'mb-category-active' : ''}`}
          onClick={() => setActiveCategory('all')}
        >
          全部
        </button>
        {CATEGORIES.map((c) => (
          <button
            key={c.key}
            className={`mb-category ${activeCategory === c.key ? 'mb-category-active' : ''}`}
            onClick={() => setActiveCategory(c.key)}
          >
            {c.label}
          </button>
        ))}
      </div>

      <div className="mb-list">
        {filtered.map((m) => (
          <button
            key={m.id}
            className="mb-card"
            onClick={() => onSelectMaterial(m)}
          >
            <div className="mb-card-title">{m.title}</div>
            <div className="mb-card-tension">{m.coreTension}</div>
            <div className="mb-card-tags">
              {m.tags.map(tag => (
                <span key={tag} className="mb-tag">{tag}</span>
              ))}
            </div>
          </button>
        ))}
      </div>
    </div>
  );
}
```

- [ ] **Step 2: 创建MaterialsBrowse样式**

```css
.materials-browse {
  padding: 0;
}

.mb-header {
  display: flex;
  align-items: center;
  gap: 12px;
  margin-bottom: 24px;
}

.mb-back {
  font-size: 14px;
  color: var(--color-body);
  background: none;
  border: none;
  cursor: pointer;
  padding: 4px 8px;
}

.mb-back:hover {
  color: var(--color-primary);
}

.mb-header h2 {
  font-size: 28px;
  font-weight: 600;
  letter-spacing: -0.5px;
  color: var(--color-primary);
}

.mb-search {
  width: 100%;
  padding: 12px 16px;
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  font-size: 14px;
  font-family: inherit;
  color: var(--color-primary);
  margin-bottom: 16px;
}

.mb-search:focus {
  outline: none;
  border-color: var(--color-accent);
}

.mb-search::placeholder {
  color: var(--color-muted);
}

.mb-categories {
  display: flex;
  gap: 8px;
  flex-wrap: wrap;
  margin-bottom: 24px;
}

.mb-category {
  padding: 8px 16px;
  border: 1px solid var(--color-border);
  border-radius: var(--radius-pill);
  background: transparent;
  font-size: 14px;
  font-weight: 500;
  color: var(--color-body);
  cursor: pointer;
  font-family: inherit;
}

.mb-category:hover {
  border-color: var(--color-primary);
  color: var(--color-primary);
}

.mb-category-active {
  background: var(--color-primary);
  border-color: var(--color-primary);
  color: var(--color-bg);
}

.mb-list {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.mb-card {
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  padding: 20px;
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  background: var(--color-bg);
  cursor: pointer;
  text-align: left;
  font-family: inherit;
  transition: border-color var(--transition-fast);
}

.mb-card:hover {
  border-color: var(--color-accent);
}

.mb-card-title {
  font-size: 18px;
  font-weight: 600;
  color: var(--color-primary);
  margin-bottom: 4px;
}

.mb-card-tension {
  font-size: 14px;
  color: var(--color-body);
  margin-bottom: 12px;
}

.mb-card-tags {
  display: flex;
  gap: 8px;
  flex-wrap: wrap;
}

.mb-tag {
  font-size: 13px;
  font-weight: 500;
  padding: 2px 8px;
  border-radius: var(--radius-sm);
  background: var(--color-tag-bg);
  color: var(--color-tag);
}
```

- [ ] **Step 3: 集成到MaterialsPage**

在MaterialsPage.tsx中添加：

```tsx
import { MaterialsBrowse } from './MaterialsBrowse';

// In render:
if (view === 'browse') {
  return (
    <MaterialsBrowse
      onSelectMaterial={handleSelectMaterial}
      onBack={() => setView('home')}
    />
  );
}
```

- [ ] **Step 4: 运行开发服务器验证**

Run: `npm run dev`
Expected: 点击"全部素材"进入浏览页，可以筛选和搜索

- [ ] **Step 5: 提交**

```bash
git add src/components/MaterialsBrowse.tsx src/components/MaterialsBrowse.css src/components/MaterialsPage.tsx
git commit -m "feat: add MaterialsBrowse with category filter and search"
```

---

## Task 10: 创建学习路径组件

**Files:**
- Create: `src/components/LearningPaths.tsx`
- Create: `src/components/LearningPaths.css`
- Create: `src/data/learningPaths.json`

- [ ] **Step 1: 创建学习路径数据**

```json
[
  {
    "id": "theme-self",
    "type": "theme",
    "name": "我与自我",
    "description": "探索自我认知、个人价值、内心冲突",
    "materialIds": ["m1", "m2", "m3"],
    "order": 1
  },
  {
    "id": "theme-others",
    "type": "theme",
    "name": "我与他人",
    "description": "理解人际关系、社会互动、道德责任",
    "materialIds": ["m4", "m5", "m6"],
    "order": 2
  },
  {
    "id": "ability-question",
    "type": "ability",
    "name": "学会质疑",
    "description": "培养批判性思维，不盲从权威",
    "materialIds": ["m1", "m7", "m8"],
    "order": 1
  },
  {
    "id": "ability-perspective",
    "type": "ability",
    "name": "多角度分析",
    "description": "从不同立场看待同一问题",
    "materialIds": ["m2", "m9", "m10"],
    "order": 2
  }
]
```

- [ ] **Step 2: 创建LearningPaths组件**

```tsx
import { useState } from 'react';
import type { Material, LearningPath as LearningPathType } from '../types';
import materials from '../data/materials.json';
import learningPathsData from '../data/learningPaths.json';
import './LearningPaths.css';

const allMaterials = materials as Material[];
const allPaths = learningPathsData as LearningPathType[];

interface LearningPathsProps {
  onSelectMaterial: (material: Material) => void;
  onBack: () => void;
}

export function LearningPaths({ onSelectMaterial, onBack }: LearningPathsProps) {
  const [activeTab, setActiveTab] = useState<'theme' | 'ability'>('theme');

  const themePaths = allPaths.filter(p => p.type === 'theme');
  const abilityPaths = allPaths.filter(p => p.type === 'ability');
  const currentPaths = activeTab === 'theme' ? themePaths : abilityPaths;

  return (
    <div className="learning-paths">
      <div className="lp-header">
        <button className="lp-back" onClick={onBack}>←</button>
        <h2>学习路径</h2>
      </div>

      <div className="lp-tabs">
        <button
          className={`lp-tab ${activeTab === 'theme' ? 'lp-tab-active' : ''}`}
          onClick={() => setActiveTab('theme')}
        >
          按主题
        </button>
        <button
          className={`lp-tab ${activeTab === 'ability' ? 'lp-tab-active' : ''}`}
          onClick={() => setActiveTab('ability')}
        >
          按能力
        </button>
      </div>

      <div className="lp-list">
        {currentPaths.map(path => (
          <div key={path.id} className="lp-path">
            <div className="lp-path-header">
              <div className="lp-path-name">{path.name}</div>
              <div className="lp-path-desc">{path.description}</div>
            </div>
            <div className="lp-materials">
              {path.materialIds.map(id => {
                const material = allMaterials.find(m => m.id === id);
                if (!material) return null;
                return (
                  <button
                    key={id}
                    className="lp-material"
                    onClick={() => onSelectMaterial(material)}
                  >
                    <span className="lp-material-title">{material.title}</span>
                    <span className="lp-material-tension">{material.coreTension}</span>
                  </button>
                );
              })}
            </div>
          </div>
        ))}
      </div>
    </div>
  );
}
```

- [ ] **Step 3: 创建LearningPaths样式**

```css
.learning-paths {
  padding: 0;
}

.lp-header {
  display: flex;
  align-items: center;
  gap: 12px;
  margin-bottom: 24px;
}

.lp-back {
  font-size: 14px;
  color: var(--color-body);
  background: none;
  border: none;
  cursor: pointer;
  padding: 4px 8px;
}

.lp-back:hover {
  color: var(--color-primary);
}

.lp-header h2 {
  font-size: 28px;
  font-weight: 600;
  letter-spacing: -0.5px;
  color: var(--color-primary);
}

.lp-tabs {
  display: flex;
  gap: 8px;
  margin-bottom: 24px;
}

.lp-tab {
  padding: 8px 16px;
  border: 1px solid var(--color-border);
  border-radius: var(--radius-pill);
  background: transparent;
  font-size: 14px;
  font-weight: 500;
  color: var(--color-body);
  cursor: pointer;
  font-family: inherit;
}

.lp-tab:hover {
  border-color: var(--color-primary);
  color: var(--color-primary);
}

.lp-tab-active {
  background: var(--color-primary);
  border-color: var(--color-primary);
  color: var(--color-bg);
}

.lp-list {
  display: flex;
  flex-direction: column;
  gap: 16px;
}

.lp-path {
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  overflow: hidden;
}

.lp-path-header {
  padding: 16px 20px;
  background: var(--color-bg-neutral);
  border-bottom: 1px solid var(--color-border-dark);
}

.lp-path-name {
  font-size: 18px;
  font-weight: 600;
  color: var(--color-primary);
  margin-bottom: 4px;
}

.lp-path-desc {
  font-size: 14px;
  color: var(--color-body);
}

.lp-materials {
  display: flex;
  flex-direction: column;
}

.lp-material {
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  padding: 12px 20px;
  border: none;
  border-bottom: 1px solid var(--color-border-dark);
  background: var(--color-bg);
  cursor: pointer;
  text-align: left;
  font-family: inherit;
  transition: background var(--transition-fast);
}

.lp-material:last-child {
  border-bottom: none;
}

.lp-material:hover {
  background: var(--color-bg-hover);
}

.lp-material-title {
  font-size: 14px;
  font-weight: 500;
  color: var(--color-primary);
  margin-bottom: 2px;
}

.lp-material-tension {
  font-size: 13px;
  color: var(--color-body);
}
```

- [ ] **Step 4: 集成到MaterialsPage**

在MaterialsPage.tsx中添加：

```tsx
import { LearningPaths } from './LearningPaths';

// In render:
if (view === 'paths') {
  return (
    <LearningPaths
      onSelectMaterial={handleSelectMaterial}
      onBack={() => setView('home')}
    />
  );
}
```

- [ ] **Step 5: 运行开发服务器验证**

Run: `npm run dev`
Expected: 点击"学习路径"进入路径页，可以切换主题和能力

- [ ] **Step 6: 提交**

```bash
git add src/components/LearningPaths.tsx src/components/LearningPaths.css src/data/learningPaths.json src/components/MaterialsPage.tsx
git commit -m "feat: add LearningPaths with theme and ability tabs"
```

---

## Task 11: 创建概念地图组件

**Files:**
- Create: `src/components/ConceptMap.tsx`
- Create: `src/components/ConceptMap.css`

- [ ] **Step 1: 创建ConceptMap组件**

```tsx
import { useState } from 'react';
import type { Material, KnowledgeConcept } from '../types';
import materials from '../data/materials.json';
import conceptsData from '../data/knowledge/concepts.json';
import './ConceptMap.css';

const allMaterials = materials as Material[];
const concepts = conceptsData as KnowledgeConcept[];

interface ConceptMapProps {
  onSelectMaterial: (material: Material) => void;
  onBack: () => void;
}

export function ConceptMap({ onSelectMaterial, onBack }: ConceptMapProps) {
  const [expandedId, setExpandedId] = useState<string | null>(null);

  return (
    <div className="concept-map">
      <div className="cm-header">
        <button className="cm-back" onClick={onBack}>←</button>
        <h2>概念地图</h2>
      </div>

      <p className="cm-subtitle">15个核心概念，帮你建立思维框架</p>

      <div className="cm-list">
        {concepts.map(concept => (
          <div key={concept.id} className="cm-concept">
            <button
              className="cm-concept-header"
              onClick={() => setExpandedId(expandedId === concept.id ? null : concept.id)}
            >
              <div className="cm-concept-name">{concept.concept}</div>
              <div className="cm-concept-hook">{concept.hook}</div>
            </button>
            {expandedId === concept.id && (
              <div className="cm-concept-detail">
                <div className="cm-detail-section">
                  <div className="cm-detail-label">分析句式</div>
                  <div className="cm-detail-text">{concept.analysisTpl}</div>
                </div>
                <div className="cm-detail-section">
                  <div className="cm-detail-label">示例</div>
                  <div className="cm-examples">
                    {concept.examples.map((ex, i) => (
                      <div key={i} className="cm-example">
                        <span className="cm-example-type">
                          {ex.type === 'daily' ? '日常' : '论据'}
                        </span>
                        <span className="cm-example-text">{ex.text}</span>
                      </div>
                    ))}
                  </div>
                </div>
                <div className="cm-detail-section">
                  <div className="cm-detail-label">相关素材</div>
                  <div className="cm-related">
                    {allMaterials
                      .filter(m => m.tags.some(t => 
                        concept.concept.includes(t) || t.includes(concept.concept)
                      ))
                      .slice(0, 3)
                      .map(m => (
                        <button
                          key={m.id}
                          className="cm-related-item"
                          onClick={() => onSelectMaterial(m)}
                        >
                          {m.title}
                        </button>
                      ))
                    }
                  </div>
                </div>
              </div>
            )}
          </div>
        ))}
      </div>
    </div>
  );
}
```

- [ ] **Step 2: 创建ConceptMap样式**

```css
.concept-map {
  padding: 0;
}

.cm-header {
  display: flex;
  align-items: center;
  gap: 12px;
  margin-bottom: 8px;
}

.cm-back {
  font-size: 14px;
  color: var(--color-body);
  background: none;
  border: none;
  cursor: pointer;
  padding: 4px 8px;
}

.cm-back:hover {
  color: var(--color-primary);
}

.cm-header h2 {
  font-size: 28px;
  font-weight: 600;
  letter-spacing: -0.5px;
  color: var(--color-primary);
}

.cm-subtitle {
  font-size: 14px;
  color: var(--color-body);
  margin-bottom: 24px;
}

.cm-list {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.cm-concept {
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  overflow: hidden;
}

.cm-concept-header {
  width: 100%;
  padding: 16px 20px;
  border: none;
  background: var(--color-bg);
  cursor: pointer;
  text-align: left;
  font-family: inherit;
  transition: background var(--transition-fast);
}

.cm-concept-header:hover {
  background: var(--color-bg-hover);
}

.cm-concept-name {
  font-size: 16px;
  font-weight: 600;
  color: var(--color-primary);
  margin-bottom: 4px;
}

.cm-concept-hook {
  font-size: 13px;
  color: var(--color-body);
}

.cm-concept-detail {
  padding: 0 20px 20px;
  border-top: 1px solid var(--color-border-dark);
}

.cm-detail-section {
  margin-top: 16px;
}

.cm-detail-label {
  font-size: 11px;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  color: var(--color-body);
  margin-bottom: 8px;
}

.cm-detail-text {
  font-size: 14px;
  line-height: 1.60;
  color: var(--color-secondary);
}

.cm-examples {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.cm-example {
  display: flex;
  gap: 8px;
  align-items: flex-start;
}

.cm-example-type {
  font-size: 11px;
  font-weight: 600;
  padding: 2px 6px;
  border-radius: var(--radius-sm);
  background: var(--color-bg-neutral);
  color: var(--color-body);
  flex-shrink: 0;
}

.cm-example-text {
  font-size: 14px;
  line-height: 1.50;
  color: var(--color-secondary);
}

.cm-related {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
}

.cm-related-item {
  padding: 6px 12px;
  border: 1px solid var(--color-border);
  border-radius: var(--radius-pill);
  background: var(--color-bg);
  font-size: 13px;
  color: var(--color-primary);
  cursor: pointer;
  font-family: inherit;
}

.cm-related-item:hover {
  border-color: var(--color-accent);
}
```

- [ ] **Step 3: 集成到MaterialsPage**

在MaterialsPage.tsx中添加：

```tsx
import { ConceptMap } from './ConceptMap';

// In render:
if (view === 'concept-map') {
  return (
    <ConceptMap
      onSelectMaterial={handleSelectMaterial}
      onBack={() => setView('home')}
    />
  );
}
```

- [ ] **Step 4: 运行开发服务器验证**

Run: `npm run dev`
Expected: 点击"概念地图"进入地图页，可以展开概念查看详情

- [ ] **Step 5: 提交**

```bash
git add src/components/ConceptMap.tsx src/components/ConceptMap.css src/components/MaterialsPage.tsx
git commit -m "feat: add ConceptMap with concept details and related materials"
```

---

## Task 12: 最终集成测试

**Files:**
- None (testing only)

- [ ] **Step 1: 运行完整功能测试**

Run: `npm run dev`

测试以下流程：
1. 进入素材模块，看到新首页
2. 点击"今日精选"进入详情页
3. 切换到"辩论模式"，选择"AI当对手"
4. 选择立场，进行辩论
5. 返回首页，点击"全部素材"
6. 搜索和筛选素材
7. 返回首页，点击"学习路径"
8. 切换"按主题"和"按能力"
9. 返回首页，点击"概念地图"
10. 展开概念查看详情

- [ ] **Step 2: 运行类型检查**

Run: `npx tsc --noEmit`
Expected: 无类型错误

- [ ] **Step 3: 运行构建**

Run: `npm run build`
Expected: 构建成功

- [ ] **Step 4: 提交最终版本**

```bash
git add .
git commit -m "feat: complete materials module redesign with debate mode and reading materials"
```

---

## 执行说明

**推荐执行方式：** Subagent-Driven Development

使用 `superpowers:subagent-driven-development` skill，每个Task由独立subagent执行，Task之间进行review。

**执行顺序：**
1. Task 1-3: 基础设施（设计变量、类型、存储）
2. Task 4-6: 核心组件（首页、辩论、阅读材料）
3. Task 7-8: 集成到现有页面
4. Task 9-11: 扩展功能（浏览、路径、概念地图）
5. Task 12: 最终测试

**预计时间：** 2-3小时（使用subagent并行执行）
