# 题库检索功能 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为写作模块题库添加简洁的渐进式筛选功能，支持主题标签和思辨类型筛选

**Architecture:** 在 questions.json 中新增 theme 字段，重构 TopicInput 组件实现搜索+筛选面板，采用按钮切换展开/收起的渐进式交互

**Tech Stack:** React 18 + TypeScript + Vite

---

## File Structure

- Modify: `src/types/index.ts` — 添加 QuestionTheme 类型，更新 Question 接口
- Modify: `src/data/questions.json` — 为 120 个题目添加 theme 字段
- Modify: `src/components/TopicInput.tsx` — 重写筛选逻辑和 UI
- Modify: `src/components/TopicInput.css` — 重写筛选样式

---

### Task 1: 更新类型定义

**Files:**
- Modify: `src/types/index.ts:1-12`

- [ ] **Step 1: 添加 QuestionTheme 类型**

在 `SpeculativeType` 类型定义后添加：

```typescript
export type QuestionTheme =
  | '科技'
  | '人文'
  | '社会'
  | '成长'
  | '文化'
  | '价值'
  | '自我'
  | '时代';
```

- [ ] **Step 2: 更新 Question 接口**

在 `Question` 接口中添加 `theme` 字段：

```typescript
export interface Question {
  id: string;
  text: string;
  source: string;
  genre: EssayGenre;
  speculativeType: SpeculativeType;
  difficulty: 1 | 2 | 3;
  tags: string[];
  theme: QuestionTheme;
}
```

- [ ] **Step 3: 验证类型编译**

Run: `npx tsc --noEmit`
Expected: 无错误输出

- [ ] **Step 4: 提交**

```bash
git add src/types/index.ts
git commit -m "feat: add QuestionTheme type and update Question interface"
```

---

### Task 2: 更新 questions.json 数据

**Files:**
- Modify: `src/data/questions.json`

- [ ] **Step 1: 为所有题目添加 theme 字段**

根据题目内容为每个题目分配主题标签：

| 主题 | 分配规则 | 示例 |
|------|----------|------|
| 科技 | AI、互联网、技术发展相关 | q1, q20, q27, q29 |
| 人文 | 故事、语言、文化传承、艺术 | q6, q14, q95, q101 |
| 社会 | 合作、竞争、社会关系、家国 | q5, q16, q37, q62 |
| 成长 | 自我认知、青春、人生体验 | q7, q50, q58, q69 |
| 文化 | 传统、创新、文明、经典 | q33, q73, q94, q101 |
| 价值 | 选择、判断、意义、道德 | q34, q43, q51, q71 |
| 自我 | 独立、自由、身份认同、内心 | q26, q46, q47, q28 |
| 时代 | 变革、未来、当下、变局 | q30, q61, q62, q20 |

更新每个题目的 JSON，添加 `"theme": "对应主题"` 字段。

- [ ] **Step 2: 验证 JSON 格式**

Run: `node -e "const data = require('./src/data/questions.json'); console.log('Total:', data.length); const themes = new Set(data.map(q => q.theme)); console.log('Themes:', [...themes]);"`

Expected: Total: 120, Themes: 8个主题标签

- [ ] **Step 3: 验证类型编译**

Run: `npx tsc --noEmit`
Expected: 无错误输出

- [ ] **Step 4: 提交**

```bash
git add src/data/questions.json
git commit -m "feat: add theme field to all 120 questions"
```

---

### Task 3: 重写 TopicInput.tsx 筛选逻辑

**Files:**
- Modify: `src/components/TopicInput.tsx`

- [ ] **Step 1: 更新导入**

```typescript
import { useState, useMemo } from 'react';
import type { Question, SpeculativeType, QuestionTheme } from '../types';
import { loadEssays } from '../services/storageService';
import questions from '../data/questions.json';
import './TopicInput.css';

const allQuestions = questions as Question[];

const THEMES: QuestionTheme[] = ['科技', '人文', '社会', '成长', '文化', '价值', '自我', '时代'];
const TYPES: SpeculativeType[] = ['single', 'binary', 'ternary'];
```

- [ ] **Step 2: 保留推荐逻辑**

保留 `getRecommendedQuestions` 函数不变。

- [ ] **Step 3: 重写组件状态和筛选逻辑**

```typescript
export function TopicInput({ onConfirm }: TopicInputProps) {
  const [mode, setMode] = useState<'pick' | 'custom'>('pick');
  const [selectedId, setSelectedId] = useState<string | null>(null);
  const [customTopic, setCustomTopic] = useState('');
  const [searchText, setSearchText] = useState('');
  const [showFilters, setShowFilters] = useState(false);
  const [selectedThemes, setSelectedThemes] = useState<QuestionTheme[]>([]);
  const [selectedTypes, setSelectedTypes] = useState<SpeculativeType[]>([]);
  const recommended = useMemo(getRecommendedQuestions, []);

  const filteredQuestions = useMemo(() => {
    const recommendedIds = new Set(recommended.map(q => q.id));
    return allQuestions.filter(q => {
      if (recommendedIds.has(q.id)) return false;
      if (searchText && !q.text.includes(searchText) && !q.source.includes(searchText)) return false;
      if (selectedThemes.length > 0 && !selectedThemes.includes(q.theme)) return false;
      if (selectedTypes.length > 0 && !selectedTypes.includes(q.speculativeType)) return false;
      return true;
    });
  }, [searchText, selectedThemes, selectedTypes, recommended]);

  const toggleTheme = (theme: QuestionTheme) => {
    setSelectedThemes(prev =>
      prev.includes(theme) ? prev.filter(t => t !== theme) : [...prev, theme]
    );
  };

  const toggleType = (type: SpeculativeType) => {
    setSelectedTypes(prev =>
      prev.includes(type) ? prev.filter(t => t !== type) : [...prev, type]
    );
  };

  const clearFilters = () => {
    setSearchText('');
    setSelectedThemes([]);
    setSelectedTypes([]);
  };

  const hasFilters = searchText || selectedThemes.length > 0 || selectedTypes.length > 0;

  const handleStart = () => {
    if (mode === 'pick' && selectedId) {
      const q = allQuestions.find((q) => q.id === selectedId);
      if (q) onConfirm(q.text, q.id);
    } else if (mode === 'custom' && customTopic.trim()) {
      onConfirm(customTopic.trim(), null);
    }
  };

  // ... JSX
}
```

- [ ] **Step 4: 验证类型编译**

Run: `npx tsc --noEmit`
Expected: 无错误输出

- [ ] **Step 5: 提交**

```bash
git add src/components/TopicInput.tsx
git commit -m "feat: rewrite TopicInput filter logic with theme and type filters"
```

---

### Task 4: 重写 TopicInput.tsx JSX

**Files:**
- Modify: `src/components/TopicInput.tsx`

- [ ] **Step 1: 重写 return JSX**

```tsx
return (
  <div className="topic-input">
    <h2 className="page-title">今天想写什么题目？</h2>
    <div className="mode-toggle">
      <button
        className={mode === 'pick' ? 'active' : ''}
        onClick={() => setMode('pick')}
      >
        从题库选
      </button>
      <button
        className={mode === 'custom' ? 'active' : ''}
        onClick={() => setMode('custom')}
      >
        自己输入
      </button>
    </div>

    {mode === 'pick' && (
      <>
        <div className="search-row">
          <input
            type="text"
            placeholder="搜索题干或来源..."
            value={searchText}
            onChange={e => setSearchText(e.target.value)}
            className="search-input"
          />
          <button
            className={`filter-toggle ${showFilters ? 'active' : ''}`}
            onClick={() => setShowFilters(!showFilters)}
          >
            筛选
          </button>
        </div>

        {showFilters && (
          <div className="filter-panel">
            <div className="filter-section">
              <span className="filter-label">主题</span>
              <div className="filter-options">
                {THEMES.map(theme => (
                  <button
                    key={theme}
                    className={`filter-btn ${selectedThemes.includes(theme) ? 'active' : ''}`}
                    onClick={() => toggleTheme(theme)}
                  >
                    {theme}
                  </button>
                ))}
              </div>
            </div>
            <div className="filter-section">
              <span className="filter-label">类型</span>
              <div className="filter-options">
                {TYPES.map(type => (
                  <button
                    key={type}
                    className={`filter-btn ${selectedTypes.includes(type) ? 'active' : ''}`}
                    onClick={() => toggleType(type)}
                  >
                    {type === 'single' ? '一元' : type === 'binary' ? '二元' : '三元'}
                  </button>
                ))}
              </div>
            </div>
            <button className="clear-filters" onClick={clearFilters}>清空筛选</button>
          </div>
        )}

        {hasFilters && (
          <div className="filter-status">
            <span>筛选结果：{filteredQuestions.length} 道题</span>
          </div>
        )}

        <div className="question-list">
          {recommended.length > 0 && (
            <div className="recommend-section">
              <span className="recommend-label">推荐练笔</span>
              {recommended.map((q) => (
                <div
                  key={q.id}
                  className={`card card-clickable ${selectedId === q.id ? 'selected' : ''}`}
                  onClick={() => setSelectedId(q.id)}
                >
                  <p className="card-desc">{q.text.slice(0, 80)}...</p>
                  <div className="card-tags">
                    <span className="card-tag">{q.theme}</span>
                    <span className="card-tag">
                      {q.speculativeType === 'single' ? '一元' : q.speculativeType === 'binary' ? '二元' : '三元'}
                    </span>
                  </div>
                </div>
              ))}
            </div>
          )}
          {filteredQuestions.map((q) => (
            <div
              key={q.id}
              className={`card card-clickable ${selectedId === q.id ? 'selected' : ''}`}
              onClick={() => setSelectedId(q.id)}
            >
              <p className="card-desc">{q.text.slice(0, 80)}...</p>
              <div className="card-tags">
                <span className="card-tag">{q.theme}</span>
                <span className="card-tag">
                  {q.speculativeType === 'single' ? '一元' : q.speculativeType === 'binary' ? '二元' : '三元'}
                </span>
              </div>
            </div>
          ))}
          {filteredQuestions.length === 0 && hasFilters && (
            <div className="empty-state">
              没有找到匹配的题目，试试调整筛选条件
            </div>
          )}
        </div>
      </>
    )}

    {mode === 'custom' && (
      <textarea
        className="form-textarea"
        placeholder="输入或粘贴作文题目..."
        value={customTopic}
        onChange={(e) => setCustomTopic(e.target.value)}
        rows={4}
      />
    )}

    <button
      className="btn-primary btn-full"
      disabled={mode === 'pick' ? !selectedId : !customTopic.trim()}
      onClick={handleStart}
    >
      开始写作
    </button>
  </div>
);
```

- [ ] **Step 2: 验证类型编译**

Run: `npx tsc --noEmit`
Expected: 无错误输出

- [ ] **Step 3: 提交**

```bash
git add src/components/TopicInput.tsx
git commit -m "feat: rewrite TopicInput JSX with filter panel and theme tags"
```

---

### Task 5: 重写 TopicInput.css 样式

**Files:**
- Modify: `src/components/TopicInput.css`

- [ ] **Step 1: 替换全部样式**

```css
.topic-input {
  padding-bottom: 24px;
}

.mode-toggle {
  display: flex;
  gap: 8px;
  margin-bottom: 20px;
}

.mode-toggle button {
  flex: 1;
  padding: 8px 16px;
  border: 1px solid var(--color-border);
  border-radius: var(--radius-pill);
  background: var(--color-bg);
  font-size: 13px;
  font-weight: 500;
  cursor: pointer;
  transition: all var(--transition-fast);
}

.mode-toggle button.active {
  background: var(--color-accent);
  color: #fff;
  border-color: var(--color-accent);
}

/* 搜索行 */
.search-row {
  display: flex;
  gap: 8px;
  margin-bottom: 16px;
}

.search-input {
  flex: 1;
  padding: 10px 14px;
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  font-size: 14px;
  background: var(--color-bg);
}

.search-input:focus {
  outline: none;
  border-color: var(--color-accent);
}

.filter-toggle {
  padding: 10px 16px;
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  background: var(--color-bg);
  font-size: 14px;
  cursor: pointer;
  transition: all var(--transition-fast);
  white-space: nowrap;
}

.filter-toggle.active {
  background: var(--color-accent);
  color: #fff;
  border-color: var(--color-accent);
}

/* 筛选面板 */
.filter-panel {
  background: var(--color-bg);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  padding: 16px;
  margin-bottom: 16px;
}

.filter-section {
  margin-bottom: 12px;
}

.filter-section:last-of-type {
  margin-bottom: 16px;
}

.filter-label {
  font-size: 12px;
  font-weight: 600;
  color: var(--color-text-secondary);
  margin-bottom: 8px;
  display: block;
}

.filter-options {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
}

.filter-btn {
  padding: 6px 12px;
  border: 1px solid var(--color-border);
  border-radius: var(--radius-pill);
  background: var(--color-bg);
  font-size: 13px;
  cursor: pointer;
  transition: all var(--transition-fast);
}

.filter-btn.active {
  background: var(--color-accent);
  color: #fff;
  border-color: var(--color-accent);
}

.clear-filters {
  color: var(--color-accent);
  cursor: pointer;
  font-size: 12px;
  background: none;
  border: none;
  padding: 0;
}

/* 筛选状态 */
.filter-status {
  margin-bottom: 12px;
  font-size: 12px;
  color: var(--color-text-secondary);
}

/* 题目列表 */
.question-list {
  max-height: 360px;
  overflow-y: auto;
  display: flex;
  flex-direction: column;
  gap: 8px;
  margin-bottom: 20px;
}

.question-list .card.selected {
  border-color: var(--color-accent);
  background: var(--color-accent-soft);
}

.recommend-section {
  margin-bottom: 16px;
}

.recommend-label {
  display: inline-block;
  font-size: 11px;
  font-weight: 600;
  color: var(--color-accent);
  background: var(--color-accent-soft);
  padding: 4px 10px;
  border-radius: var(--radius-sm);
  margin-bottom: 8px;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.empty-state {
  padding: 20px;
  text-align: center;
  color: var(--color-text-secondary);
  font-size: 14px;
}
```

- [ ] **Step 2: 验证构建**

Run: `npm run build`
Expected: 构建成功，无错误

- [ ] **Step 3: 提交**

```bash
git add src/components/TopicInput.css
git commit -m "feat: rewrite TopicInput CSS with filter panel styles"
```

---

### Task 6: 集成验证

**Files:**
- None (verification only)

- [ ] **Step 1: TypeScript 检查**

Run: `npx tsc --noEmit`
Expected: 无错误输出

- [ ] **Step 2: 生产构建**

Run: `npm run build`
Expected: 构建成功

- [ ] **Step 3: 功能验证清单**

启动开发服务器 `npm run dev`，验证：
- [ ] 搜索框输入关键词实时过滤题目
- [ ] 点击[筛选]按钮展开筛选面板
- [ ] 再次点击[筛选]按钮收起筛选面板
- [ ] 点击主题标签切换选中状态
- [ ] 点击思辨类型标签切换选中状态
- [ ] 多选标签组合筛选
- [ ] 清空筛选按钮重置所有条件
- [ ] 筛选结果计数正确显示
- [ ] 推荐练笔区域始终显示
- [ ] 推荐题目不重复出现在筛选结果中
- [ ] 无筛选时显示全部题目（除推荐外）
- [ ] 移动端布局正常

- [ ] **Step 4: 最终提交**

```bash
git add -A
git commit -m "feat: complete question bank search and filter feature"
```
