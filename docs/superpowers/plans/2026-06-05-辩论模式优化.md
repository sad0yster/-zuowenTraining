# 辩论模式优化实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 重构辩论模式为阶段式引导的思辨工具，使用 Mintlify 设计语言，支持自由切换立场和自动收获卡。

**Architecture:** 删除旧的裁判/对手双模式，统一为单一辩论流程。前端控制阶段推进（concept→opposition→synthesis→application），AI 负责对话质量和阶段引导。数据模型扩展支持阶段追踪和收获卡存储。

**Tech Stack:** React 18 + TypeScript + Vite，无新依赖。样式遵循 mintlify/DESIGN.md 设计系统。

---

### Task 1: 更新类型定义

**Files:**
- Modify: `src/types/index.ts:170-189`

- [ ] **Step 1: 更新辩论类型**

将 `src/types/index.ts` 中的辩论类型替换为：

```typescript
// Debate
export type DebateStage = 'concept' | 'opposition' | 'synthesis' | 'application';
export type DebateSide = 'for' | 'against';

export interface DebateRound {
  round: number;
  side: DebateSide;
  content: string;
  isUser: boolean;
  timestamp: number;
  stage: DebateStage;
}

export interface DebateRecord {
  id: string;
  materialId: string;
  rounds: DebateRound[];
  currentStage: DebateStage;
  completedStages: DebateStage[];
  harvest?: string;
  createdAt: number;
}
```

删除 `DebateMode` 类型。

- [ ] **Step 2: 类型检查**

Run: `npx tsc --noEmit`
Expected: 报错（因为其他文件还在引用旧类型），记录报错位置，后续任务修复。

- [ ] **Step 3: 提交**

```bash
git add src/types/index.ts
git commit -m "refactor: update debate types for staged debate flow"
```

---

### Task 2: 重写辩论 Prompt

**Files:**
- Rewrite: `src/prompts/debate.ts`

- [ ] **Step 1: 重写 debate.ts**

替换 `src/prompts/debate.ts` 全部内容为：

```typescript
import type { Material, DebateStage, DebateSide } from '../types';

const STAGE_NAMES: Record<DebateStage, string> = {
  concept: '概念界定',
  opposition: '寻找对立面',
  synthesis: '更高维度',
  application: '现实联系',
};

const STAGE_TRANSITIONS: Partial<Record<DebateStage, string>> = {
  concept: '我们已经界定了核心概念的含义。现在，让我们试试完全站在反面——如果有人坚决反对你的观点，他们会怎么说？',
  opposition: '你已经看到了正反两面。现在问自己：这两种看似矛盾的观点，有没有可能指向同一个更深层的东西？',
  synthesis: '这个更高维度的理解，你觉得在现实生活中有什么具体的例子？',
};

export const DEBATE_PROMPTS = {
  opening: (material: Material) => `
你是一个思辨教练。现在开始一场关于「${material.title}」的辩论。

情境：${material.situation}
核心张力：${material.coreTension}

请用1-2句话引入辩论，然后抛出一个概念界定问题，引导学生思考核心概念的含义。

要求：
- 用苏格拉底式提问
- 问题要具体，不要太抽象
- 引导学生从定义开始思考
- 长度100-150字
`.trim(),

  respond: (
    material: Material,
    userArgument: string,
    userSide: DebateSide,
    stage: DebateStage,
    round: number,
    sideSwitched: boolean,
  ) => {
    const sideName = userSide === 'for' ? '正方' : '反方';
    const oppositeName = userSide === 'for' ? '反方' : '正方';
    const stageName = STAGE_NAMES[stage];
    const transition = round >= 2 ? STAGE_TRANSITIONS[stage] : '';

    const switchHint = sideSwitched
      ? `\n注意：用户刚刚从${oppositeName}切换到${sideName}立场。请先指出切换视角本身的价值，问他们发现了什么之前没注意到的东西。`
      : '';

    const scaffoldHint = userArgument.length < 50
      ? `\n注意：用户的论点比较简短。请在回应末尾给出迷你框架：「你的论点可以从三个角度展开：定义、证据、推理。先试试从定义开始？」`
      : '';

    return `
你是一个思辨教练，通过辩论形式帮助学生深度拆解素材。

素材：${material.title}
情境：${material.situation}
核心张力：${material.coreTension}

当前阶段：${stageName}
用户立场：${sideName}
已进行轮次：${round}
${switchHint}

用户论点：
${userArgument}

请从${oppositeName}的角度回应用户。要求：
1. 直接回应用户的论点，指出其中的隐含假设或逻辑漏洞
2. 提出新的视角或反例
3. 长度150-200字
4. 用苏格拉底式提问，不要直接给答案
${scaffoldHint}
${transition ? `\n如果合适，在末尾自然引导下一阶段：${transition}` : ''}
`.trim();
  },

  harvest: (material: Material, rounds: { round: number; side: DebateSide; content: string }[]) => `
请基于以下辩论记录，生成一张收获卡。

素材：${material.title}
核心张力：${material.coreTension}

辩论记录：
${rounds.map(r => `第${r.round}轮 [${r.side === 'for' ? '正方' : '反方'}]：${r.content}`).join('\n\n')}

请生成收获卡，包含以下三部分：

【核心洞察】
通过这场辩论，我们发现了什么深层思考？（2-3句话，回扣素材核心张力）

【可用论据】
从辩论中提取2-3个最有价值的论据，标注来源轮次和立场。（可直接用在作文中）

【辩证统一】
看似矛盾的两种观点，其实指向什么更高维度的理解？（2-3句话）

要求：
- 提炼而非复述，每条收获都要有"可直接用在作文里"的实用价值
- 引用具体轮次中的高光时刻
- 回扣素材的核心张力
`.trim(),
};
```

- [ ] **Step 2: 类型检查**

Run: `npx tsc --noEmit`
Expected: 仍报错（aiService 和 storageService 还未更新），记录报错位置。

- [ ] **Step 3: 提交**

```bash
git add src/prompts/debate.ts
git commit -m "refactor: rewrite debate prompts for staged guidance"
```

---

### Task 3: 更新 aiService

**Files:**
- Modify: `src/services/aiService.ts:435-441`

- [ ] **Step 1: 更新 sendDebateMessage**

将 `src/services/aiService.ts` 中的 `sendDebateMessage` 函数替换为：

```typescript
export async function sendDebateMessage(prompt: string): Promise<string> {
  const apiMessages: ApiMessage[] = [
    { role: 'system', content: '你是一个善于辩论的思辨教练。请用中文回应。你的目标不是赢得辩论，而是帮助学生通过辩论形式深度拆解素材。' },
    { role: 'user', content: prompt },
  ];
  return callLLM(apiMessages);
}
```

- [ ] **Step 2: 类型检查**

Run: `npx tsc --noEmit`
Expected: 仍报错（storageService 和组件还未更新）。

- [ ] **Step 3: 提交**

```bash
git add src/services/aiService.ts
git commit -m "refactor: update debate message system prompt"
```

---

### Task 4: 更新 storageService

**Files:**
- Modify: `src/services/storageService.ts:120-147`

- [ ] **Step 1: 更新存储函数**

`saveDebateRecord` 和 `loadDebateRecords` 函数签名不需要改（类型自动跟随 `DebateRecord`）。但需要确保旧记录兼容——删除 `mode` 字段的引用。

在 `src/services/storageService.ts` 中，确认 `saveDebateRecord`、`loadDebateRecords`、`loadDebateRecordsByMaterial`、`clearDebateRecord` 函数不变，类型会自动更新。

- [ ] **Step 2: 类型检查**

Run: `npx tsc --noEmit`
Expected: 仍报错（组件还未更新）。

- [ ] **Step 3: 提交**

```bash
git add src/services/storageService.ts
git commit -m "refactor: align storage service with new debate types"
```

---

### Task 5: 更新 MaterialDetail

**Files:**
- Modify: `src/components/MaterialDetail.tsx:244-263`

- [ ] **Step 1: 移除辩论子模式选择 UI**

在 `src/components/MaterialDetail.tsx` 中，保留「自由讨论 / 辩论模式」的切换（第244-260行），但删除辩论模式内部的「用户当裁判 / AI当对手」选择。

当前代码中 `debateMode` 是 boolean，直接控制是否渲染 `<DebateMode>` 组件，这部分不变。`DebateMode` 组件内部的模式选择将在 Task 6 中删除。

确认 MaterialDetail 中的 import 和渲染逻辑不需要改动（`DebateMode` 组件的 props 接口不变：`material` + `onBack`）。

- [ ] **Step 2: 类型检查**

Run: `npx tsc --noEmit`
Expected: 仍报错（DebateMode 组件还未更新）。

- [ ] **Step 3: 提交**

```bash
git add src/components/MaterialDetail.tsx
git commit -m "refactor: prepare MaterialDetail for new debate flow"
```

---

### Task 6: 重写 DebateMode 组件

**Files:**
- Rewrite: `src/components/DebateMode.tsx`

- [ ] **Step 1: 重写 DebateMode.tsx**

替换 `src/components/DebateMode.tsx` 全部内容为：

```tsx
import { useState, useRef, useEffect } from 'react';
import type { Material, DebateStage, DebateSide, DebateRound } from '../types';
import { generateId, sendDebateMessage } from '../services/aiService';
import { DEBATE_PROMPTS } from '../prompts/debate';
import { saveDebateRecord } from '../services/storageService';
import { LoadingDots } from './LoadingDots';
import './DebateMode.css';

interface DebateModeProps {
  material: Material;
  onBack: () => void;
}

const STAGES: { key: DebateStage; label: string }[] = [
  { key: 'concept', label: '概念界定' },
  { key: 'opposition', label: '寻找对立' },
  { key: 'synthesis', label: '更高维度' },
  { key: 'application', label: '现实联系' },
];

function getStageForRound(round: number): DebateStage {
  if (round <= 2) return 'concept';
  if (round <= 4) return 'opposition';
  if (round <= 6) return 'synthesis';
  return 'application';
}

function getCompletedStages(rounds: DebateRound[]): DebateStage[] {
  const stageRoundCounts = new Map<DebateStage, number>();
  for (const r of rounds) {
    stageRoundCounts.set(r.stage, (stageRoundCounts.get(r.stage) || 0) + 1);
  }
  const completed: DebateStage[] = [];
  for (const [stage, count] of stageRoundCounts) {
    if (count >= 2) completed.push(stage);
  }
  return completed;
}

export function DebateMode({ material, onBack }: DebateModeProps) {
  const [rounds, setRounds] = useState<DebateRound[]>([]);
  const [currentRound, setCurrentRound] = useState(1);
  const [input, setInput] = useState('');
  const [loading, setLoading] = useState(false);
  const [harvest, setHarvest] = useState<string | null>(null);
  const [userSide, setUserSide] = useState<DebateSide>('for');
  const [prevSide, setPrevSide] = useState<DebateSide | null>(null);
  const [guideMessage, setGuideMessage] = useState<string | null>(null);
  const [showEndPrompt, setShowEndPrompt] = useState(false);
  const bottomRef = useRef<HTMLDivElement>(null);

  const currentStage = getStageForRound(currentRound);
  const completedStages = getCompletedStages(rounds);

  useEffect(() => {
    bottomRef.current?.scrollIntoView({ behavior: 'smooth' });
  }, [rounds, guideMessage]);

  // Auto-send opening prompt
  useEffect(() => {
    const openingPrompt = DEBATE_PROMPTS.opening(material);
    sendDebateMessage(openingPrompt).then((response) => {
      setGuideMessage(response);
    });
  }, []);

  const handleSend = async () => {
    const trimmed = input.trim();
    if (!trimmed || loading || harvest) return;

    const sideSwitched = prevSide !== null && prevSide !== userSide;
    const stage = getStageForRound(currentRound);

    const userRound: DebateRound = {
      round: currentRound,
      side: userSide,
      content: trimmed,
      isUser: true,
      timestamp: Date.now(),
      stage,
    };

    setRounds((prev) => [...prev, userRound]);
    setInput('');
    setPrevSide(userSide);
    setLoading(true);

    try {
      const prompt = DEBATE_PROMPTS.respond(
        material,
        trimmed,
        userSide,
        stage,
        currentRound,
        sideSwitched,
      );
      const response = await sendDebateMessage(prompt);

      const aiRound: DebateRound = {
        round: currentRound,
        side: userSide === 'for' ? 'against' : 'for',
        content: response,
        isUser: false,
        timestamp: Date.now(),
        stage,
      };
      setRounds((prev) => [...prev, aiRound]);
      setCurrentRound((prev) => prev + 1);

      // Check end conditions: 8 rounds total or all 4 stages have 2+ rounds
      const allDone = completedStages.length >= 4;
      if (allDone || currentRound >= 8) {
        setShowEndPrompt(true);
      }
    } catch {
      setRounds((prev) => [
        ...prev,
        {
          round: currentRound,
          side: userSide === 'for' ? 'against' : 'for',
          content: '抱歉，出了点问题。请稍后再试。',
          isUser: false,
          timestamp: Date.now(),
          stage,
        },
      ]);
    } finally {
      setLoading(false);
    }
  };

  const handleEndDebate = async () => {
    setShowEndPrompt(false);
    setLoading(true);
    try {
      const harvestPrompt = DEBATE_PROMPTS.harvest(material, rounds);
      const harvestText = await sendDebateMessage(harvestPrompt);
      setHarvest(harvestText);

      // Save record
      saveDebateRecord({
        id: generateId(),
        materialId: material.id,
        rounds,
        currentStage,
        completedStages,
        harvest: harvestText,
        createdAt: Date.now(),
      });
    } catch {
      setHarvest('生成收获卡时出错，请重试。');
    } finally {
      setLoading(false);
    }
  };

  const handleCopyHarvest = () => {
    if (harvest) {
      navigator.clipboard.writeText(harvest);
    }
  };

  const handleKeyDown = (e: React.KeyboardEvent) => {
    if (e.key === 'Enter' && !e.shiftKey) {
      e.preventDefault();
      handleSend();
    }
  };

  return (
    <div className="debate-mode">
      {/* Header */}
      <div className="dm-header">
        <button className="dm-back" onClick={onBack}>
          &larr;
        </button>
        <h3 className="dm-title">{material.title}</h3>
      </div>

      {/* Stage indicator */}
      <div className="dm-stages">
        {STAGES.map((s) => (
          <span
            key={s.key}
            className={`dm-stage ${
              s.key === currentStage
                ? 'dm-stage-active'
                : completedStages.includes(s.key)
                ? 'dm-stage-done'
                : ''
            }`}
          >
            {completedStages.includes(s.key) && s.key !== currentStage ? '✓ ' : ''}
            {s.label}
          </span>
        ))}
      </div>

      {/* Debate card */}
      <div className="dm-card">
        {/* Guide message */}
        {guideMessage && (
          <div className="dm-guide">{guideMessage}</div>
        )}

        {/* Rounds */}
        {rounds.length > 0 && (
          <div className="dm-rounds">
            {rounds.map((round, index) => (
              <div key={index} className="dm-round">
                <div className="dm-round-label">
                  <span className={`dm-round-side ${round.side === 'for' ? 'dm-round-side-for' : 'dm-round-side-against'}`}>
                    {round.side === 'for' ? '正方' : '反方'}
                  </span>
                  <span className="dm-round-meta">
                    {round.isUser ? '我' : 'AI'} · 第{round.round}轮
                  </span>
                </div>
                <div className="dm-round-content">{round.content}</div>
              </div>
            ))}
            {loading && <LoadingDots />}
          </div>
        )}

        {/* End prompt */}
        {showEndPrompt && (
          <div className="dm-end-prompt">
            <p>你想继续深入，还是总结今天的收获？</p>
            <div className="dm-end-buttons">
              <button className="dm-btn-secondary" onClick={() => setShowEndPrompt(false)}>
                继续辩论
              </button>
              <button className="dm-btn-primary" onClick={handleEndDebate}>
                总结收获
              </button>
            </div>
          </div>
        )}

        {/* Input area */}
        {!harvest && !showEndPrompt && (
          <div className="dm-input-area">
            <div className="dm-side-tabs">
              <button
                className={`dm-side-tab ${userSide === 'for' ? 'dm-side-tab-active' : ''}`}
                onClick={() => setUserSide('for')}
              >
                正方
              </button>
              <button
                className={`dm-side-tab ${userSide === 'against' ? 'dm-side-tab-active' : ''}`}
                onClick={() => setUserSide('against')}
              >
                反方
              </button>
            </div>
            <textarea
              className="dm-input"
              value={input}
              onChange={(e) => setInput(e.target.value)}
              onKeyDown={handleKeyDown}
              placeholder={`写下你的${userSide === 'for' ? '正方' : '反方'}论点...`}
              disabled={loading}
            />
            <div className="dm-input-footer">
              <button
                className="dm-btn-primary"
                onClick={handleSend}
                disabled={loading || !input.trim()}
              >
                提交
              </button>
            </div>
          </div>
        )}

        {/* Harvest card */}
        {harvest && (
          <div className="dm-harvest">
            <div className="dm-harvest-content">
              {harvest.split('\n').map((line, i) => (
                <p
                  key={i}
                  className={line.startsWith('【') ? 'dm-harvest-heading' : 'dm-harvest-text'}
                >
                  {line}
                </p>
              ))}
            </div>
            <div className="dm-harvest-actions">
              <button className="dm-btn-secondary" onClick={handleCopyHarvest}>
                复制
              </button>
              <button className="dm-btn-primary" onClick={onBack}>
                保存并返回
              </button>
            </div>
          </div>
        )}

        <div ref={bottomRef} />
      </div>
    </div>
  );
}
```

- [ ] **Step 2: 类型检查**

Run: `npx tsc --noEmit`
Expected: 无报错（如果前面的类型都已更新）。

- [ ] **Step 3: 提交**

```bash
git add src/components/DebateMode.tsx
git commit -m "feat: rewrite DebateMode with staged guidance and Mintlify UI"
```

---

### Task 7: 重写 DebateMode.css

**Files:**
- Rewrite: `src/components/DebateMode.css`

- [ ] **Step 1: 重写样式**

替换 `src/components/DebateMode.css` 全部内容为：

```css
/* Mintlify Design Tokens */
.debate-mode {
  --dm-primary: #0a0a0a;
  --dm-on-primary: #ffffff;
  --dm-accent: #00d4a4;
  --dm-accent-deep: #00b48a;
  --dm-surface: #f7f7f7;
  --dm-canvas: #ffffff;
  --dm-hairline: #e5e5e5;
  --dm-hairline-soft: #ededed;
  --dm-ink: #0a0a0a;
  --dm-steel: #5a5a5c;
  --dm-muted: #a8a8aa;
  --dm-radius-lg: 12px;
  --dm-radius-md: 8px;
  --dm-radius-sm: 6px;
  --dm-radius-pill: 9999px;
  --dm-spacing-xs: 8px;
  --dm-spacing-sm: 12px;
  --dm-spacing-md: 16px;
  --dm-spacing-lg: 20px;
  --dm-spacing-xl: 24px;
  --dm-spacing-xxl: 32px;
}

/* Header */
.dm-header {
  display: flex;
  align-items: center;
  gap: var(--dm-spacing-sm);
  margin-bottom: var(--dm-spacing-xl);
}

.dm-back {
  font-size: 14px;
  color: var(--dm-steel);
  background: none;
  border: none;
  cursor: pointer;
  padding: 4px 8px;
  font-family: inherit;
}

.dm-back:hover {
  color: var(--dm-ink);
}

.dm-title {
  font-family: Inter, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
  font-size: 28px;
  font-weight: 600;
  letter-spacing: -0.5px;
  color: var(--dm-ink);
  margin: 0;
}

/* Stage indicator */
.dm-stages {
  display: flex;
  gap: var(--dm-spacing-xs);
  margin-bottom: var(--dm-spacing-xl);
  overflow-x: auto;
  -webkit-overflow-scrolling: touch;
}

.dm-stage {
  font-family: Inter, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
  font-size: 12px;
  font-weight: 500;
  padding: 6px 14px;
  border-radius: var(--dm-radius-pill);
  background: var(--dm-canvas);
  color: var(--dm-steel);
  border: 1px solid var(--dm-hairline);
  white-space: nowrap;
  cursor: default;
}

.dm-stage-active {
  background: var(--dm-accent);
  color: var(--dm-primary);
  border-color: var(--dm-accent);
  font-weight: 600;
}

.dm-stage-done {
  background: var(--dm-surface);
  color: var(--dm-accent-deep);
  border-color: var(--dm-accent);
}

/* Debate card */
.dm-card {
  border: 1px solid var(--dm-hairline);
  border-radius: var(--dm-radius-lg);
  overflow: hidden;
}

/* Guide message */
.dm-guide {
  padding: var(--dm-spacing-lg);
  background: var(--dm-surface);
  border-left: 2px solid var(--dm-accent);
  font-family: Inter, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
  font-size: 14px;
  line-height: 1.60;
  color: var(--dm-ink);
}

/* Rounds */
.dm-rounds {
  padding: var(--dm-spacing-lg);
  max-height: 500px;
  overflow-y: auto;
}

.dm-round {
  margin-bottom: var(--dm-spacing-md);
  padding: var(--dm-spacing-md);
  border-radius: var(--dm-radius-md);
  border: 1px solid var(--dm-hairline-soft);
}

.dm-round-label {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: var(--dm-spacing-xs);
}

.dm-round-side {
  font-family: Inter, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
  font-size: 12px;
  font-weight: 600;
  padding: 2px 8px;
  border-radius: var(--dm-radius-sm);
}

.dm-round-side-for {
  background: rgba(0, 212, 164, 0.1);
  color: var(--dm-accent-deep);
}

.dm-round-side-against {
  background: var(--dm-surface);
  color: var(--dm-steel);
}

.dm-round-meta {
  font-family: Inter, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
  font-size: 12px;
  color: var(--dm-muted);
}

.dm-round-content {
  font-family: Inter, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
  font-size: 14px;
  line-height: 1.60;
  color: var(--dm-ink);
}

/* End prompt */
.dm-end-prompt {
  padding: var(--dm-spacing-lg);
  background: var(--dm-surface);
  text-align: center;
}

.dm-end-prompt p {
  font-family: Inter, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
  font-size: 14px;
  color: var(--dm-ink);
  margin: 0 0 var(--dm-spacing-md);
}

.dm-end-buttons {
  display: flex;
  gap: var(--dm-spacing-xs);
  justify-content: center;
}

/* Input area */
.dm-input-area {
  padding: var(--dm-spacing-lg);
  border-top: 1px solid var(--dm-hairline-soft);
}

.dm-side-tabs {
  display: flex;
  gap: var(--dm-spacing-xs);
  margin-bottom: var(--dm-spacing-sm);
}

.dm-side-tab {
  font-family: Inter, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
  font-size: 13px;
  font-weight: 500;
  padding: 6px 14px;
  border-radius: var(--dm-radius-pill);
  background: var(--dm-canvas);
  color: var(--dm-steel);
  border: 1px solid var(--dm-hairline);
  cursor: pointer;
}

.dm-side-tab:hover {
  border-color: var(--dm-ink);
  color: var(--dm-ink);
}

.dm-side-tab-active {
  background: var(--dm-primary);
  color: var(--dm-on-primary);
  border-color: var(--dm-primary);
}

.dm-input {
  width: 100%;
  padding: var(--dm-spacing-sm) var(--dm-spacing-md);
  border: 1px solid var(--dm-hairline);
  border-radius: var(--dm-radius-md);
  font-family: Inter, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
  font-size: 14px;
  line-height: 1.50;
  color: var(--dm-ink);
  resize: none;
  height: 120px;
  box-sizing: border-box;
}

.dm-input:focus {
  outline: none;
  border-color: var(--dm-accent);
}

.dm-input::placeholder {
  color: var(--dm-muted);
}

.dm-input-footer {
  display: flex;
  justify-content: flex-end;
  margin-top: var(--dm-spacing-sm);
}

/* Harvest card */
.dm-harvest {
  padding: var(--dm-spacing-lg);
  border-top: 1px solid var(--dm-hairline-soft);
  border-left: 2px solid var(--dm-accent);
}

.dm-harvest-content {
  margin-bottom: var(--dm-spacing-lg);
}

.dm-harvest-heading {
  font-family: Inter, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
  font-size: 15px;
  font-weight: 600;
  color: var(--dm-ink);
  margin: var(--dm-spacing-md) 0 var(--dm-spacing-xs);
}

.dm-harvest-heading:first-child {
  margin-top: 0;
}

.dm-harvest-text {
  font-family: Inter, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
  font-size: 14px;
  line-height: 1.60;
  color: var(--dm-ink);
  margin: 0 0 var(--dm-spacing-xs);
}

.dm-harvest-actions {
  display: flex;
  gap: var(--dm-spacing-xs);
  justify-content: flex-end;
}

/* Buttons */
.dm-btn-primary {
  font-family: Inter, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
  font-size: 14px;
  font-weight: 500;
  padding: 10px 20px;
  border-radius: var(--dm-radius-pill);
  background: var(--dm-primary);
  color: var(--dm-on-primary);
  border: none;
  cursor: pointer;
}

.dm-btn-primary:hover:not(:disabled) {
  background: #1c1c1e;
}

.dm-btn-primary:disabled {
  background: var(--dm-hairline);
  color: var(--dm-muted);
  cursor: not-allowed;
}

.dm-btn-secondary {
  font-family: Inter, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
  font-size: 14px;
  font-weight: 500;
  padding: 10px 20px;
  border-radius: var(--dm-radius-pill);
  background: transparent;
  color: var(--dm-ink);
  border: 1px solid var(--dm-hairline);
  cursor: pointer;
}

.dm-btn-secondary:hover {
  border-color: var(--dm-ink);
}

/* Responsive */
@media (max-width: 480px) {
  .dm-stages {
    gap: 4px;
  }

  .dm-stage {
    font-size: 11px;
    padding: 4px 10px;
  }

  .dm-title {
    font-size: 22px;
  }
}
```

- [ ] **Step 2: 类型检查**

Run: `npx tsc --noEmit`
Expected: 无报错。

- [ ] **Step 3: 提交**

```bash
git add src/components/DebateMode.css
git commit -m "feat: add Mintlify-style debate mode CSS"
```

---

### Task 8: 最终验证

**Files:**
- None (verification only)

- [ ] **Step 1: 类型检查**

Run: `npx tsc --noEmit`
Expected: 无报错。

- [ ] **Step 2: 构建验证**

Run: `npm run build`
Expected: 构建成功，无错误。

- [ ] **Step 3: 开发服务器验证**

Run: `npm run dev`
Expected: 服务器启动成功，浏览器打开 http://localhost:3000，进入素材详情页，点击辩论模式，验证：
- 开场引导自动显示
- 阶段指示器正常
- 立场切换标签正常
- 输入论点后 AI 回应正常
- 收获卡生成正常

- [ ] **Step 4: 最终提交**

```bash
git add -A
git commit -m "feat: complete debate mode optimization with staged guidance and Mintlify UI"
```
