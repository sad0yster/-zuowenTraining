# 阶段二：训练体系 实现计划

> **For agentic workers:** Implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax.

**Goal:** 在 MVP 自由写作台之上加入刻意练习体系、Tab 导航、基础成长追踪

**Architecture:** App.tsx 升级为 Tab 路由容器（TabBar + 四个页面），训练五个单元共享一个通用 TrainingUnit shell（选题 → 练习 → AI 对话反馈），我的页面从 localStorage 读取训练记录统计

**Tech Stack:** React 18 + TypeScript + CSS（与 MVP 一致），无新增依赖

---

## 文件结构（阶段二变更）

```
Modified:
  src/App.tsx              → Tab 容器，替换单页 WritingDesk
  src/types/index.ts       → 新增训练/成长类型
  src/prompts/index.ts     → 新增五个训练单元 prompt
  src/services/aiService.ts → 新增训练单元 AI 调用
  src/services/storageService.ts → 新增训练记录存取
  src/data/questions.json  → 扩充至 80 题

New:
  src/components/TabBar.tsx + .css
  src/components/TrainingPage.tsx + .css    → 五单元卡片 + 最近记录
  src/components/TrainingUnit.tsx           → 通用训练单元（选题→练习→反馈）
  src/components/MyPage.tsx + .css          → 思维快照 + 训练足迹
```

---

### Task 1: 类型定义扩展 + 题库扩充

**Files:**
- Modify: `src/types/index.ts`
- Modify: `src/data/questions.json`

- [ ] **Step 1: 扩展类型定义**

在 `src/types/index.ts` 中新增：

```typescript
// 训练单元类型
export type DrillType = 'examining' | 'thesis' | 'titling' | 'reasoning' | 'perspective';

export interface DrillRecord {
  id: string;
  type: DrillType;
  questionId: string | null;
  questionText: string;
  studentInput: string;
  aiFeedback: string;
  createdAt: number;
}

// Tab 导航
export type TabId = 'materials' | 'training' | 'writing' | 'me';

// 训练页 Tab 内的子步骤
export type DrillStep = 'topic-select' | 'practice' | 'feedback';
```

- [ ] **Step 2: 扩充题库至 80 题**

在 `src/data/questions.json` 中追加 70 道新题（从原有 10 道扩至 80 道），覆盖更多二元/三元思辨类型和高阶议题。新增题目按 spec 标记文体/思辨类型/难度/标签。（完整 JSON 见计划文件末尾附录）

- [ ] **Step 3: TypeScript 编译检查**

```bash
npx tsc --noEmit
```
Expected: 无错误


### Task 2: Bottom Tab 导航

**Files:**
- Create: `src/components/TabBar.tsx`, `src/components/TabBar.css`
- Modify: `src/App.tsx`

- [ ] **Step 1: 创建 TabBar 组件**

```typescript
import type { TabId } from '../types';
import './TabBar.css';

interface TabBarProps {
  active: TabId;
  onChange: (tab: TabId) => void;
}

const TABS: { id: TabId; label: string }[] = [
  { id: 'materials', label: '素材' },
  { id: 'training', label: '训练' },
  { id: 'writing', label: '写作' },
  { id: 'me', label: '我的' },
];

export function TabBar({ active, onChange }: TabBarProps) {
  return (
    <nav className="tab-bar">
      {TABS.map((tab) => (
        <button
          key={tab.id}
          className={`tab-item ${active === tab.id ? 'active' : ''}`}
          onClick={() => onChange(tab.id)}
        >
          {tab.label}
        </button>
      ))}
    </nav>
  );
}
```

- [ ] **Step 2: TabBar.css**

```css
.tab-bar {
  display: flex;
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  max-width: 720px;
  margin: 0 auto;
  background: #fff;
  border-top: 1px solid #eee;
}

.tab-item {
  flex: 1;
  padding: 12px 0;
  border: none;
  background: none;
  font-size: 14px;
  color: #999;
  cursor: pointer;
}

.tab-item.active {
  color: #1a1a1a;
  font-weight: 600;
}
```

- [ ] **Step 3: 升级 App.tsx 为 Tab 容器**

```typescript
import { useState } from 'react';
import type { TabId } from './types';
import { TabBar } from './components/TabBar';
import { TrainingPage } from './components/TrainingPage';
import { WritingDesk } from './components/WritingDesk';
import { MyPage } from './components/MyPage';
import './App.css';

function App() {
  const [activeTab, setActiveTab] = useState<TabId>('training');

  return (
    <div className="app">
      <header className="app-header">
        <h1>高分作文训练</h1>
      </header>
      <main className="app-main">
        {activeTab === 'materials' && (
          <div className="placeholder-page">素材库即将上线</div>
        )}
        {activeTab === 'training' && <TrainingPage />}
        {activeTab === 'writing' && <WritingDesk />}
        {activeTab === 'me' && <MyPage />}
      </main>
      <TabBar active={activeTab} onChange={setActiveTab} />
    </div>
  );
}

export default App;
```

- [ ] **Step 4: App.css 追加**

```css
.app-main {
  padding: 24px;
  padding-bottom: 72px; /* tab bar height */
}

.placeholder-page {
  text-align: center;
  padding: 80px 0;
  color: #999;
  font-size: 14px;
}
```

- [ ] **Step 5: 验证**

```bash
npm run dev
```
打开浏览器确认底部 4 个 Tab 显示正常，点击切换可用。写作 Tab 功能完整（原有 MVP 流程）。


### Task 3: 训练页（五单元入口 + 最近记录）

**Files:**
- Create: `src/components/TrainingPage.tsx`, `src/components/TrainingPage.css`

- [ ] **Step 1: 创建 TrainingPage**

```typescript
import { useState } from 'react';
import type { DrillType, DrillRecord } from '../types';
import { loadDrillRecords } from '../services/storageService';
import { TrainingUnit } from './TrainingUnit';
import './TrainingPage.css';

const DRILLS: { type: DrillType; label: string; desc: string }[] = [
  {
    type: 'examining',
    label: '审题拆解',
    desc: '拆关键概念、找隐含前提、列多向立意',
  },
  {
    type: 'thesis',
    label: '立意突破',
    desc: '同一题目的三层立意对比和升维',
  },
  {
    type: 'titling',
    label: '命题训练',
    desc: '同一立意拟多个标题并自评优劣',
  },
  {
    type: 'reasoning',
    label: '论证链',
    desc: '理由→证据→推理→反驳逐环检测',
  },
  {
    type: 'perspective',
    label: '视角切换',
    desc: '同一主题用不同概念框架各写一段',
  },
];

export function TrainingPage() {
  const [activeDrill, setActiveDrill] = useState<DrillType | null>(null);
  const recentRecords: DrillRecord[] = loadDrillRecords().slice(-5).reverse();

  if (activeDrill) {
    return (
      <TrainingUnit
        type={activeDrill}
        onBack={() => setActiveDrill(null)}
        onRefresh={() => {}}
      />
    );
  }

  return (
    <div className="training-page">
      <h2>思辨练习场</h2>
      <p className="training-subtitle">每次 5-10 分钟，专攻一个得分点</p>

      <div className="drill-grid">
        {DRILLS.map((d) => (
          <button
            key={d.type}
            className="drill-card"
            onClick={() => setActiveDrill(d.type)}
          >
            <span className="drill-label">{d.label}</span>
            <span className="drill-desc">{d.desc}</span>
          </button>
        ))}
      </div>

      {recentRecords.length > 0 && (
        <section className="recent-section">
          <h3>最近训练</h3>
          {recentRecords.slice(0, 5).map((r) => (
            <div key={r.id} className="recent-item">
              <span className="recent-type">
                {DRILLS.find((d) => d.type === r.type)?.label}
              </span>
              <span className="recent-topic">{r.questionText.slice(0, 40)}...</span>
            </div>
          ))}
        </section>
      )}
    </div>
  );
}
```

- [ ] **Step 2: TrainingPage.css**

```css
.training-page h2 {
  font-size: 18px;
  margin-bottom: 4px;
}

.training-subtitle {
  font-size: 13px;
  color: #999;
  margin-bottom: 24px;
}

.drill-grid {
  display: flex;
  flex-direction: column;
  gap: 10px;
  margin-bottom: 32px;
}

.drill-card {
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  padding: 16px;
  border: 1px solid #eee;
  border-radius: 10px;
  background: #fff;
  cursor: pointer;
  text-align: left;
}

.drill-card:hover {
  border-color: #1a1a1a;
}

.drill-label {
  font-size: 15px;
  font-weight: 600;
  margin-bottom: 4px;
}

.drill-desc {
  font-size: 13px;
  color: #666;
}

.recent-section h3 {
  font-size: 14px;
  color: #999;
  margin-bottom: 12px;
}

.recent-item {
  display: flex;
  gap: 12px;
  padding: 8px 0;
  border-bottom: 1px solid #f5f5f5;
  font-size: 13px;
}

.recent-type {
  color: #1a1a1a;
  font-weight: 600;
  flex-shrink: 0;
}

.recent-topic {
  color: #666;
}
```

- [ ] **Step 3: 更新 storageService.ts 添加训练记录存取**

```typescript
import type { DrillRecord } from '../types';

const DRILL_KEY = 'zuowen-training-drills';

export function saveDrillRecord(record: DrillRecord): void {
  const existing = loadDrillRecords();
  existing.push(record);
  localStorage.setItem(DRILL_KEY, JSON.stringify(existing));
}

export function loadDrillRecords(): DrillRecord[] {
  try {
    const raw = localStorage.getItem(DRILL_KEY);
    return raw ? JSON.parse(raw) : [];
  } catch {
    return [];
  }
}
```

- [ ] **Step 4: tsc 检查**

```bash
npx tsc --noEmit
```


### Task 4: 通用训练单元 + 五个单元 prompt

**Files:**
- Create: `src/components/TrainingUnit.tsx`
- Modify: `src/prompts/index.ts`
- Modify: `src/services/aiService.ts`

- [ ] **Step 1: 新增五个训练单元 prompt 到 prompts/index.ts**

```typescript
export const DRILL_PROMPTS: Record<string, string> = {
  examining: `[examining]
你正在帮学生进行审题拆解训练。给出一道作文题，学生需要拆解关键概念、找隐含前提、列出可能的立意方向。

你的引导策略：
1. 先让学生用自己的话解释题目中每个关键概念
2. 追问："这道题有没有隐含的前提假设？"
3. 引导列出至少 3 个可能的立意角度
4. 对于学生没触及的角度，用提问引导——"如果从XX的角度看呢？"
5. 最后帮学生判断：哪些角度是"切合题意"的，哪些是"偏离"的

注意：不直接告诉学生应该怎么立意，引导他们自己发现。`,

  thesis: `[thesis]
你正在帮学生进行立意突破训练。给出一道作文题，学生在你的引导下逐步看到平庸→较好→高级立意的差异。

你的引导策略：
1. 先让学生说出自己第一时间想到的立意
2. 指出这个立意的层次（通常第一反应是平庸的），并展示一个更高一层的角度
3. 解释"为什么这一层比那一层更高"——不是因为用词更好，而是因为看到了更根本的矛盾或更深的结构
4. 引导学生在更高视点上重新思考

核心：不做概念讲授，而是通过具体题目的立意对比，让学生自己"看到"层次差异。`,

  titling: `[titling]
你正在帮学生进行命题训练。给定一个明确的立意，学生需要拟多个不同层次的标题。

你的引导策略：
1. 先让学生拟一个标题
2. 追问："这个标题暗示读者什么期待？正文能兑现吗？"
3. 让学生尝试 2-3 个不同层级的标题（告知型→表态型→张力型）
4. 帮学生比较这些标题的差异——不是评"哪个好"，而是"哪个给了读者更强烈的阅读动机"`,

  reasoning: `[reasoning]
你正在帮学生进行论证链训练。给出一个论点和一段论证，你需要逐环检测论证链条。

你的引导策略：
1. 让学生拆解自己的论证：主张是什么？理由是什么？证据是什么？推理是什么？
2. 逐环追问："这个理由真的支持主张吗？""这个证据能推出这个结论吗？""中间有没有跳跃？"
3. 假设一个反例，测试论证是否成立
4. 指出最常见的论证漏洞：以例代证、循环论证、偷换概念、以偏概全`,

  perspective: `[perspective]
你正在帮学生进行视角切换训练。给一个主题，学生从不同概念框架各写一段分析。

你的引导策略：
1. 先让学生用自己最习惯的视角写一段
2. 然后引导切换："试试从XX的视角看同一件事"（根据题目动态匹配视角，如经济学视角、伦理学视角、历史视角、结构视角等）
3. 让学生比较两段的差异——不是评"哪个对"，而是感受不同视角带来的不同洞察`,
};
```

- [ ] **Step 2: 新增训练单元 AI 调用到 aiService.ts**

```typescript
import { DRILL_PROMPTS } from '../prompts';
import type { DrillType } from '../types';

export async function sendDrillMessage(
  drillType: DrillType,
  questionText: string,
  studentInput: string
): Promise<string> {
  const systemPrompt =
    ROLE_PROMPT + '\n\n' + (DRILL_PROMPTS[drillType] || '') +
    '\n\n当前训练的作文题目是：' + questionText;
  const apiMessages: ApiMessage[] = [
    { role: 'system', content: systemPrompt },
    { role: 'user', content: studentInput },
  ];
  return callLLM(apiMessages);
}
```

- [ ] **Step 3: 创建 TrainingUnit 通用组件**

```typescript
import { useState } from 'react';
import type { DrillType, DrillRecord } from '../types';
import { generateId, sendDrillMessage } from '../services/aiService';
import { saveDrillRecord } from '../services/storageService';
import questions from '../data/questions.json';
import type { Question } from '../types';
import { ChatBubble } from './ChatBubble';

interface TrainingUnitProps {
  type: DrillType;
  onBack: () => void;
  onRefresh: () => void;
}

const UNIT_LABELS: Record<DrillType, string> = {
  examining: '审题拆解',
  thesis: '立意突破',
  titling: '命题训练',
  reasoning: '论证链',
  perspective: '视角切换',
};

const allQuestions = questions as Question[];

export function TrainingUnit({ type, onBack, onRefresh }: TrainingUnitProps) {
  const [step, setStep] = useState<'topic-select' | 'practice' | 'feedback'>('topic-select');
  const [selectedTopic, setSelectedTopic] = useState<string>('');
  const [studentInput, setStudentInput] = useState('');
  const [aiResponse, setAiResponse] = useState('');
  const [loading, setLoading] = useState(false);

  const handleSelectTopic = (text: string) => {
    setSelectedTopic(text);
    setStep('practice');
  };

  const handleSubmit = async () => {
    if (!studentInput.trim() || loading) return;
    setLoading(true);
    try {
      const reply = await sendDrillMessage(type, selectedTopic, studentInput.trim());
      setAiResponse(reply);

      const record: DrillRecord = {
        id: generateId(),
        type,
        questionId: null,
        questionText: selectedTopic,
        studentInput: studentInput.trim(),
        aiFeedback: reply,
        createdAt: Date.now(),
      };
      saveDrillRecord(record);
      setStep('feedback');
    } catch {
      setAiResponse('抱歉，出了点问题。请稍后再试。');
      setStep('feedback');
    } finally {
      setLoading(false);
    }
  };

  const handleDone = () => {
    onRefresh();
    onBack();
  };

  if (step === 'topic-select') {
    return (
      <div className="training-unit">
        <div className="tu-header">
          <button className="tu-back" onClick={onBack}>←</button>
          <h3>{UNIT_LABELS[type]}</h3>
        </div>
        <p className="tu-prompt">选择一道题来练习</p>
        <div className="tu-topic-list">
          {allQuestions.slice(0, 10).map((q) => (
            <button
              key={q.id}
              className="tu-topic-card"
              onClick={() => handleSelectTopic(q.text)}
            >
              {q.text.slice(0, 60)}...
            </button>
          ))}
        </div>
      </div>
    );
  }

  if (step === 'practice') {
    return (
      <div className="training-unit">
        <div className="tu-header">
          <button className="tu-back" onClick={() => setStep('topic-select')}>←</button>
          <h3>{UNIT_LABELS[type]}</h3>
        </div>
        <div className="tu-topic-banner">{selectedTopic}</div>
        <textarea
          className="tu-input"
          value={studentInput}
          onChange={(e) => setStudentInput(e.target.value)}
          placeholder="在这里写下你的分析..."
          rows={8}
          autoFocus
        />
        <button
          className="tu-submit"
          onClick={handleSubmit}
          disabled={loading || !studentInput.trim()}
        >
          {loading ? '教练思考中...' : '提交'}
        </button>
      </div>
    );
  }

  return (
    <div className="training-unit">
      <div className="tu-header">
        <button className="tu-back" onClick={onBack}>←</button>
        <h3>{UNIT_LABELS[type]}</h3>
      </div>
      <div className="tu-feedback">
        <ChatBubble message={{ id: 'ai', role: 'coach', content: aiResponse, timestamp: Date.now() }} />
      </div>
      <button className="tu-done" onClick={handleDone}>完成</button>
    </div>
  );
}
```

- [ ] **Step 4: 验证**

```bash
npx tsc --noEmit
npm run dev
```
操作：点击"训练"Tab → 选择一个训练单元 → 选题 → 输入分析 → 提交 → 看到 AI 反馈 → 完成返回。


### Task 5: 我的页面（基础版）

**Files:**
- Create: `src/components/MyPage.tsx`, `src/components/MyPage.css`

- [ ] **Step 1: 创建 MyPage**

```typescript
import { loadEssays, loadDrillRecords } from '../services/storageService';
import type { DrillType } from '../types';
import './MyPage.css';

const DRILL_LABELS: Record<DrillType, string> = {
  examining: '审题拆解',
  thesis: '立意突破',
  titling: '命题训练',
  reasoning: '论证链',
  perspective: '视角切换',
};

export function MyPage() {
  const essays = loadEssays();
  const drills = loadDrillRecords();

  // Drill counts per type
  const drillCounts: Partial<Record<DrillType, number>> = {};
  drills.forEach((d) => {
    drillCounts[d.type] = (drillCounts[d.type] || 0) + 1;
  });

  const totalWords = essays.reduce(
    (sum, e) => sum + e.content.replace(/\s/g, '').length, 0
  );

  return (
    <div className="my-page">
      <h2>我的成长</h2>

      <section className="my-stats">
        <div className="my-stat">
          <span className="my-stat-value">{essays.length}</span>
          <span className="my-stat-label">完整作文</span>
        </div>
        <div className="my-stat">
          <span className="my-stat-value">{drills.length}</span>
          <span className="my-stat-label">专项训练</span>
        </div>
        <div className="my-stat">
          <span className="my-stat-value">{totalWords}</span>
          <span className="my-stat-label">累计字数</span>
        </div>
      </section>

      <section className="my-section">
        <h3>训练足迹</h3>
        {(Object.keys(DRILL_LABELS) as DrillType[]).map((type) => (
          <div key={type} className="footprint-row">
            <span className="fp-label">{DRILL_LABELS[type]}</span>
            <div className="fp-bar-wrap">
              <div
                className="fp-bar"
                style={{
                  width: `${Math.min((drillCounts[type] || 0) * 20, 100)}%`,
                }}
              />
            </div>
            <span className="fp-count">{drillCounts[type] || 0}</span>
          </div>
        ))}
      </section>

      {essays.length > 0 && (
        <section className="my-section">
          <h3>最近作文</h3>
          {essays.slice(-3).reverse().map((e) => (
            <div key={e.id} className="recent-essay">
              <span className="re-essay-topic">{e.topic.slice(0, 40)}...</span>
              <span className="re-essay-words">
                {e.content.replace(/\s/g, '').length} 字
              </span>
            </div>
          ))}
        </section>
      )}
    </div>
  );
}
```

- [ ] **Step 2: MyPage.css**

```css
.my-page h2 {
  font-size: 18px;
  margin-bottom: 24px;
}

.my-stats {
  display: flex;
  gap: 24px;
  margin-bottom: 32px;
}

.my-stat {
  text-align: center;
}

.my-stat-value {
  font-size: 28px;
  font-weight: 700;
  display: block;
}

.my-stat-label {
  font-size: 12px;
  color: #999;
}

.my-section {
  margin-bottom: 24px;
}

.my-section h3 {
  font-size: 14px;
  color: #999;
  margin-bottom: 12px;
}

.footprint-row {
  display: flex;
  align-items: center;
  gap: 12px;
  margin-bottom: 8px;
}

.fp-label {
  font-size: 13px;
  width: 70px;
  flex-shrink: 0;
}

.fp-bar-wrap {
  flex: 1;
  height: 6px;
  background: #f0f0f0;
  border-radius: 3px;
  overflow: hidden;
}

.fp-bar {
  height: 100%;
  background: #1a1a1a;
  border-radius: 3px;
  transition: width 0.3s;
}

.fp-count {
  font-size: 13px;
  color: #666;
  width: 20px;
  text-align: right;
}

.recent-essay {
  display: flex;
  justify-content: space-between;
  padding: 8px 0;
  border-bottom: 1px solid #f5f5f5;
  font-size: 13px;
}

.re-essay-topic {
  color: #1a1a1a;
}

.re-essay-words {
  color: #999;
  flex-shrink: 0;
}
```

- [ ] **Step 3: 验证**

```bash
npx tsc --noEmit && npm run dev
```
完成一次完整写作和一次训练后，检查"我的"页面数据显示正确。


### Task 6: TrainingUnit 样式 + 集成收尾

**Files:**
- Create: `src/components/TrainingUnit.css`

- [ ] **Step 1: TrainingUnit.css**

```css
.training-unit {
  display: flex;
  flex-direction: column;
  min-height: calc(100vh - 140px);
}

.tu-header {
  display: flex;
  align-items: center;
  gap: 12px;
  margin-bottom: 20px;
}

.tu-back {
  padding: 4px 8px;
  border: none;
  background: none;
  font-size: 18px;
  cursor: pointer;
}

.tu-header h3 {
  font-size: 16px;
}

.tu-prompt {
  font-size: 14px;
  color: #666;
  margin-bottom: 16px;
}

.tu-topic-list {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.tu-topic-card {
  padding: 12px;
  border: 1px solid #eee;
  border-radius: 8px;
  background: #fff;
  font-size: 14px;
  text-align: left;
  cursor: pointer;
}

.tu-topic-card:hover {
  border-color: #1a1a1a;
}

.tu-topic-banner {
  padding: 12px;
  background: #f9f9f9;
  border-radius: 8px;
  margin-bottom: 16px;
  font-size: 14px;
}

.tu-input {
  flex: 1;
  width: 100%;
  padding: 16px;
  border: 1px solid #eee;
  border-radius: 8px;
  font-size: 15px;
  line-height: 1.8;
  font-family: inherit;
  resize: vertical;
}

.tu-input:focus {
  outline: none;
  border-color: #1a1a1a;
}

.tu-submit {
  width: 100%;
  padding: 12px;
  margin-top: 12px;
  border: none;
  border-radius: 8px;
  background: #1a1a1a;
  color: #fff;
  font-size: 14px;
  cursor: pointer;
}

.tu-submit:disabled {
  background: #ccc;
  cursor: not-allowed;
}

.tu-feedback {
  flex: 1;
  padding: 16px 0;
}

.tu-done {
  width: 100%;
  padding: 12px;
  border: none;
  border-radius: 8px;
  background: #1a1a1a;
  color: #fff;
  font-size: 14px;
  cursor: pointer;
}
```

- [ ] **Step 2: 最终集成验证**

```bash
npx tsc --noEmit
npm run dev
```

端到端测试：
1. 打开应用 → 训练 Tab 可见
2. 点击"审题拆解" → 选一道题 → 输入分析 → 提交 → 看到 AI 反馈 → 完成
3. 切换到"写作"Tab → MVP 流程完整可用
4. 切换到"我的"Tab → 训练记录和作文统计正确显示
5. 切换到"素材"Tab → 显示占位提示

---

## 阶段二完成标准

- [x] 底部四 Tab 导航可用
- [x] 训练页五个单元卡片 + 最近训练记录
- [x] 每个训练单元：选题 → 输入分析 → AI 反馈 → 保存记录
- [x] 我的页：累计统计 + 训练足迹柱状图 + 最近作文
- [x] 五个训练单元各有独立 prompt（审题/立意/命题/论证/视角）
- [x] 写作 Tab 保持 MVP 完整功能
- [x] 题库扩充至 80 题
- [x] 素材 Tab 占位（阶段三实现）

---

## 附录：新增题库（70 道补充题，合并为 80 道）

因篇幅限制，新增 70 道题目采用以下批量结构。每道题格式为：

```json
{"id":"qN","text":"...","genre":"argumentative","speculativeType":"binary|single|ternary","difficulty":1|2|3,"tags":["...","..."]}
```

新增题目覆盖以下主题领域（具体题目文本省略，按以下分布设计）：
- 二元思辨（20 题）：传统与现代、个人与集体、理想与现实、等等
- 三元思辨（10 题）：本手妙手俗手类、继承创新融合类
- 一元深化（15 题）：单一概念的多维度挖掘
- 概念辨析（15 题）：常混淆的概念对
- 社会议题（10 题）：代际、技术伦理、教育公平等

实际题目内容在实施时由 AI 批量生成并插入 questions.json。
