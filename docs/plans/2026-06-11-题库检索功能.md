# 题库检索功能 实现计划

**Goal:** 给写作模块的题库选择页面添加检索功能，方便从60道题中快速找到目标题目

**Scope:** 修改 TopicInput 组件，添加关键词搜索和多维筛选

---

## 筛选维度

1. **关键词搜索** — 搜索题干文本、来源
2. **思辨类型** — 一元/二元/三元
3. **难度等级** — 基础/进阶/挑战
4. **标签筛选** — 常见标签快捷选择

---

### Task 1: 添加筛选状态和搜索逻辑

**Modify:** `src/components/TopicInput.tsx`

添加 `useState` 管理筛选条件：
```typescript
const [searchText, setSearchText] = useState('');
const [filterType, setFilterType] = useState<SpeculativeType | ''>('');
const [filterDifficulty, setFilterDifficulty] = useState<number | ''>('');
const [filterTag, setFilterTag] = useState('');
```

添加 `useMemo` 计算筛选结果：
```typescript
const filteredQuestions = useMemo(() => {
  return allQuestions.filter(q => {
    if (searchText && !q.text.includes(searchText) && !q.source.includes(searchText)) return false;
    if (filterType && q.speculativeType !== filterType) return false;
    if (filterDifficulty && q.difficulty !== filterDifficulty) return false;
    if (filterTag && !q.tags.includes(filterTag)) return false;
    return true;
  });
}, [searchText, filterType, filterDifficulty, filterTag]);
```

提取常用标签列表：
```typescript
const popularTags = useMemo(() => {
  const tagCount = new Map<string, number>();
  allQuestions.forEach(q => q.tags.forEach(t => tagCount.set(t, (tagCount.get(t) || 0) + 1)));
  return [...tagCount.entries()].sort((a, b) => b[1] - a[1]).slice(0, 10).map(([t]) => t);
}, []);
```

---

### Task 2: 添加筛选 UI

**Modify:** `src/components/TopicInput.tsx`

在推荐区域下方、题目列表上方添加筛选区域：
```jsx
<div className="search-filters">
  <input
    type="text"
    placeholder="搜索题干或来源..."
    value={searchText}
    onChange={e => setSearchText(e.target.value)}
    className="search-input"
  />
  <div className="filter-row">
    <select value={filterType} onChange={e => setFilterType(e.target.value as SpeculativeType | '')}>
      <option value="">全部类型</option>
      <option value="single">一元思辨</option>
      <option value="binary">二元思辨</option>
      <option value="ternary">三元思辨</option>
    </select>
    <select value={filterDifficulty} onChange={e => setFilterDifficulty(e.target.value ? Number(e.target.value) : '')}>
      <option value="">全部难度</option>
      <option value="1">基础</option>
      <option value="2">进阶</option>
      <option value="3">挑战</option>
    </select>
  </div>
  <div className="tag-filters">
    {popularTags.map(tag => (
      <button
        key={tag}
        className={`tag-btn ${filterTag === tag ? 'active' : ''}`}
        onClick={() => setFilterTag(filterTag === tag ? '' : tag)}
      >
        {tag}
      </button>
    ))}
  </div>
</div>
```

将题目列表渲染数据源从 `allQuestions` 改为 `filteredQuestions`。

添加筛选结果计数和清空筛选按钮。

---

### Task 3: 添加筛选样式

**Modify:** `src/components/TopicInput.css`

```css
.search-filters {
  margin-bottom: 16px;
}

.search-input {
  width: 100%;
  padding: 10px 14px;
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  font-size: 14px;
  background: var(--color-bg);
  box-sizing: border-box;
}

.search-input:focus {
  outline: none;
  border-color: var(--color-accent);
}

.filter-row {
  display: flex;
  gap: 8px;
  margin-top: 10px;
}

.filter-row select {
  flex: 1;
  padding: 8px 12px;
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  font-size: 13px;
  background: var(--color-bg);
  cursor: pointer;
}

.tag-filters {
  display: flex;
  flex-wrap: wrap;
  gap: 6px;
  margin-top: 10px;
}

.tag-btn {
  padding: 4px 10px;
  border: 1px solid var(--color-border);
  border-radius: var(--radius-pill);
  background: var(--color-bg);
  font-size: 12px;
  cursor: pointer;
  transition: all var(--transition-fast);
}

.tag-btn.active {
  background: var(--color-accent);
  color: #fff;
  border-color: var(--color-accent);
}

.filter-status {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 8px;
  font-size: 12px;
  color: var(--color-text-secondary);
}

.clear-filters {
  color: var(--color-accent);
  cursor: pointer;
  font-size: 12px;
  background: none;
  border: none;
}
```

---

### Task 4: 集成验证

```bash
npx tsc --noEmit && npm run build
```

验证：
- 搜索框输入关键词实时过滤
- 下拉选择思辨类型、难度等级
- 点击标签快捷筛选
- 组合筛选条件
- 清空筛选按钮
- 推荐区域不受筛选影响
- 移动端布局正常
