# 自由写作台 MVP 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 完成自由写作台完整闭环——学生输入题目 → AI 对话引导审题 → 独立写作 → 提交后对话式复盘

**Architecture:** React + TypeScript 单页应用，useState 状态机驱动四个步骤流转，AI 对话通过 LLM API + 分层 prompt 实现，用户数据存 localStorage，题库为静态 JSON。

**Tech Stack:** Vite + React 18 + TypeScript, CSS Modules, OpenAI-compatible API, localStorage

---

## 文件结构

```
zuowenTraining/
├── index.html
├── package.json
├── tsconfig.json
├── vite.config.ts
├── src/
│   ├── main.tsx
│   ├── App.tsx
│   ├── App.css
│   ├── types/
│   │   └── index.ts
│   ├── data/
│   │   └── questions.json
│   ├── prompts/
│   │   └── index.ts
│   ├── services/
│   │   ├── aiService.ts
│   │   └── storageService.ts
│   └── components/
│       ├── TopicInput.tsx
│       ├── TopicInput.css
│       ├── PreWriteChat.tsx
│       ├── ChatBubble.tsx
│       ├── ChatBubble.css
│       ├── Editor.tsx
│       ├── Editor.css
│       ├── PostWriteReview.tsx
│       └── WritingDesk.tsx
```

**职责边界**：
- `WritingDesk.tsx` — 状态机，管理四个步骤切换，保存全流程数据
- `TopicInput.tsx` — 提供题库选择 + 自定义输入双模式
- `PreWriteChat.tsx` — 写前 AI 对话，复用 ChatBubble
- `Editor.tsx` — 纯写作区，右下角浮动"问教练"按钮
- `PostWriteReview.tsx` — 写后对话式复盘，复用 ChatBubble
- `ChatBubble.tsx` — 一条消息的展示组件
- `aiService.ts` — 封装 API 调用，注入分层 prompt
- `storageService.ts` — 封装 localStorage 存取
- `prompts/index.ts` — 角色层 + 场景层 prompt 模板
- `data/questions.json` — 第一阶段 10 道题库
- `types/index.ts` — 共享类型定义

---

### Task 1: 项目脚手架与类型定义

**Files:**
- Create: `package.json`, `tsconfig.json`, `vite.config.ts`, `index.html`
- Create: `src/main.tsx`, `src/vite-env.d.ts`
- Create: `src/types/index.ts`
- Create: `src/App.tsx`, `src/App.css`

- [ ] **Step 1: 创建 package.json**

```json
{
  "name": "zuowen-training",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "typescript": "^5.5.3",
    "vite": "^5.3.4"
  }
}
```

- [ ] **Step 2: 创建 tsconfig.json**

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "isolatedModules": true,
    "moduleDetection": "force",
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "forceConsistentCasingInFileNames": true
  },
  "include": ["src"]
}
```

- [ ] **Step 3: 创建 vite.config.ts**

```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  server: {
    port: 3000,
    open: true,
  },
});
```

- [ ] **Step 4: 创建 index.html**

```html
<!DOCTYPE html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>高分作文训练</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

- [ ] **Step 5: 创建 src/types/index.ts**

```typescript
// 题库题目
export type EssayGenre = 'argumentative' | 'narrative';
export type SpeculativeType = 'single' | 'binary' | 'ternary';

export interface Question {
  id: string;
  text: string;
  genre: EssayGenre;
  speculativeType: SpeculativeType;
  difficulty: 1 | 2 | 3; // 1=易 2=中 3=难
  tags: string[];
}

// 聊天消息
export type MessageRole = 'user' | 'coach';

export interface ChatMessage {
  id: string;
  role: MessageRole;
  content: string;
  timestamp: number;
}

// 写作台步骤
export type WritingStep = 'topic-input' | 'pre-write' | 'writing' | 'post-write';

// 思维快照
export interface ThinkingSnapshot {
  originality: number;   // 1-5 立意原创性
  reasoning: number;     // 1-5 论证严密性
  perspective: number;   // 1-5 视角多元度
  structure: number;     // 1-5 结构完整性
  language: number;      // 1-5 语言表达力
}

// 完整作文记录
export interface EssayRecord {
  id: string;
  questionId: string | null;
  topic: string;
  content: string;
  preWriteMessages: ChatMessage[];
  postWriteMessages: ChatMessage[];
  snapshot: ThinkingSnapshot;
  createdAt: number;
}
```

- [ ] **Step 6: 创建 src/vite-env.d.ts**

```typescript
/// <reference types="vite/client" />
```

- [ ] **Step 7: 创建 src/App.tsx**

```typescript
import { WritingDesk } from './components/WritingDesk';
import './App.css';

function App() {
  return (
    <div className="app">
      <header className="app-header">
        <h1>高分作文训练</h1>
      </header>
      <main className="app-main">
        <WritingDesk />
      </main>
    </div>
  );
}

export default App;
```

- [ ] **Step 8: 创建 src/App.css**

```css
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'PingFang SC',
    'Hiragino Sans GB', 'Microsoft YaHei', sans-serif;
  background: #f5f5f5;
  color: #1a1a1a;
  line-height: 1.6;
}

.app {
  max-width: 720px;
  margin: 0 auto;
  min-height: 100vh;
  background: #fff;
}

.app-header {
  padding: 16px 24px;
  border-bottom: 1px solid #eee;
}

.app-header h1 {
  font-size: 20px;
  font-weight: 600;
}

.app-main {
  padding: 24px;
}
```

- [ ] **Step 9: 创建 src/main.tsx**

```typescript
import { StrictMode } from 'react';
import { createRoot } from 'react-dom/client';
import App from './App';

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <App />
  </StrictMode>
);
```

- [ ] **Step 10: 安装依赖并验证脚手架**

```bash
npm install
npm run dev
```
Expected: 浏览器打开 http://localhost:3000，显示 "高分作文训练" 标题


---

### Task 2: 题库与题目选择

**Files:**
- Create: `src/data/questions.json`
- Create: `src/components/TopicInput.tsx`, `src/components/TopicInput.css`
- Modify: `src/components/WritingDesk.tsx` (create)

- [ ] **Step 1: 创建题库 src/data/questions.json**

```json
[
  {
    "id": "q1",
    "text": "有人说，现代人的生活越来越便捷了；也有人说，便捷让生活失去了深度。你怎么看？请写一篇文章。",
    "genre": "argumentative",
    "speculativeType": "binary",
    "difficulty": 2,
    "tags": ["科技与生活", "现代性", "二元思辨"]
  },
  {
    "id": "q2",
    "text": "阅读下面的材料，根据要求写作。\n有人说，人终其一生都活在别人的眼光里；也有人说，最大的自由是做自己。\n请围绕\"自我与他人的目光\"这一主题，写一篇文章。",
    "genre": "argumentative",
    "speculativeType": "binary",
    "difficulty": 2,
    "tags": ["自我与他人", "自由", "身份认同"]
  },
  {
    "id": "q3",
    "text": "以\"坚持\"为题写一篇文章。",
    "genre": "argumentative",
    "speculativeType": "single",
    "difficulty": 1,
    "tags": ["个人品质", "一元思辨"]
  },
  {
    "id": "q4",
    "text": "有人说，竞争让人进步；也有人说，合作让人类走得更远。请围绕\"竞争与合作\"写一篇文章。",
    "genre": "argumentative",
    "speculativeType": "binary",
    "difficulty": 1,
    "tags": ["社会关系", "二元思辨"]
  },
  {
    "id": "q5",
    "text": "阅读下面的材料，根据要求写作。\n人们因为技术发展得以掌控时间，但也有人成了时间的仆人。\n请就\"人与技术\"的关系，写一篇文章。",
    "genre": "argumentative",
    "speculativeType": "binary",
    "difficulty": 3,
    "tags": ["技术与时间", "异化", "二元思辨"]
  },
  {
    "id": "q6",
    "text": "有人说，传统是创新的包袱；也有人说，传统是创新的根基。你怎么看？",
    "genre": "argumentative",
    "speculativeType": "binary",
    "difficulty": 2,
    "tags": ["传承与创新", "文化", "二元思辨"]
  },
  {
    "id": "q7",
    "text": "以\"我是谁\"为题，写一篇文章。",
    "genre": "argumentative",
    "speculativeType": "single",
    "difficulty": 3,
    "tags": ["自我认知", "身份认同", "存在"]
  },
  {
    "id": "q8",
    "text": "有人说，人生最重要的是结果；也有人说，过程比结果更有意义。你怎么看？",
    "genre": "argumentative",
    "speculativeType": "binary",
    "difficulty": 1,
    "tags": ["人生意义", "过程与结果", "二元思辨"]
  },
  {
    "id": "q9",
    "text": "阅读下面的材料，根据要求写作。\n\"平凡\"不等于\"平庸\"。前者是一种姿态，后者是一种放弃。\n你怎么理解？请写一篇文章。",
    "genre": "argumentative",
    "speculativeType": "single",
    "difficulty": 2,
    "tags": ["概念辨析", "平凡与平庸", "价值判断"]
  },
  {
    "id": "q10",
    "text": "有人说，自由是想做什么就做什么；也有人说，真正的自由是有能力不做不想做的事。请围绕\"自由\"写一篇文章。",
    "genre": "argumentative",
    "speculativeType": "binary",
    "difficulty": 2,
    "tags": ["自由", "概念辨析", "二元思辨"]
  }
]
```

- [ ] **Step 2: 创建题目选择组件 src/components/TopicInput.tsx**

```typescript
import { useState } from 'react';
import type { Question } from '../types';
import questions from '../data/questions.json';
import './TopicInput.css';

const allQuestions = questions as Question[];

interface TopicInputProps {
  onConfirm: (topic: string, questionId: string | null) => void;
}

export function TopicInput({ onConfirm }: TopicInputProps) {
  const [mode, setMode] = useState<'pick' | 'custom'>('pick');
  const [selectedId, setSelectedId] = useState<string | null>(null);
  const [customTopic, setCustomTopic] = useState('');

  const handleStart = () => {
    if (mode === 'pick' && selectedId) {
      const q = allQuestions.find((q) => q.id === selectedId);
      if (q) onConfirm(q.text, q.id);
    } else if (mode === 'custom' && customTopic.trim()) {
      onConfirm(customTopic.trim(), null);
    }
  };

  return (
    <div className="topic-input">
      <h2>今天想写什么题目？</h2>
      <div className="mode-toggle">
        <button
          className={mode === 'pick' ? 'active' : ''}
          onClick={() => setMode('pick')}
        >
          从题库选
        </button>
        <button
          className={mode === 'custom' ? 'active' : ''}
          onClick={() => setMode('custom')}
        >
          自己输入
        </button>
      </div>

      {mode === 'pick' && (
        <div className="question-list">
          {allQuestions.map((q) => (
            <div
              key={q.id}
              className={`question-card ${selectedId === q.id ? 'selected' : ''}`}
              onClick={() => setSelectedId(q.id)}
            >
              <p className="question-text">{q.text.slice(0, 80)}...</p>
              <span className="question-tag">
                {q.difficulty === 1 ? '基础' : q.difficulty === 2 ? '进阶' : '挑战'}
                {' · '}
                {q.tags[0]}
              </span>
            </div>
          ))}
        </div>
      )}

      {mode === 'custom' && (
        <textarea
          className="custom-input"
          placeholder="输入或粘贴作文题目..."
          value={customTopic}
          onChange={(e) => setCustomTopic(e.target.value)}
          rows={4}
        />
      )}

      <button
        className="start-btn"
        disabled={mode === 'pick' ? !selectedId : !customTopic.trim()}
        onClick={handleStart}
      >
        开始写作
      </button>
    </div>
  );
}
```

- [ ] **Step 3: 创建 TopicInput.css**

```css
.topic-input h2 {
  font-size: 18px;
  margin-bottom: 20px;
}

.mode-toggle {
  display: flex;
  gap: 8px;
  margin-bottom: 20px;
}

.mode-toggle button {
  flex: 1;
  padding: 8px;
  border: 1px solid #ddd;
  border-radius: 8px;
  background: #fff;
  font-size: 14px;
  cursor: pointer;
}

.mode-toggle button.active {
  background: #1a1a1a;
  color: #fff;
  border-color: #1a1a1a;
}

.question-list {
  max-height: 360px;
  overflow-y: auto;
  display: flex;
  flex-direction: column;
  gap: 8px;
  margin-bottom: 20px;
}

.question-card {
  padding: 12px;
  border: 1px solid #eee;
  border-radius: 8px;
  cursor: pointer;
}

.question-card.selected {
  border-color: #1a1a1a;
  background: #f9f9f9;
}

.question-text {
  font-size: 14px;
  margin-bottom: 4px;
}

.question-tag {
  font-size: 12px;
  color: #999;
}

.custom-input {
  width: 100%;
  padding: 12px;
  border: 1px solid #ddd;
  border-radius: 8px;
  font-size: 14px;
  font-family: inherit;
  resize: vertical;
  margin-bottom: 20px;
}

.start-btn {
  width: 100%;
  padding: 12px;
  border: none;
  border-radius: 8px;
  background: #1a1a1a;
  color: #fff;
  font-size: 16px;
  cursor: pointer;
}

.start-btn:disabled {
  background: #ccc;
  cursor: not-allowed;
}
```

- [ ] **Step 4: 创建 WritingDesk 状态机壳**

```typescript
import { useState } from 'react';
import type { WritingStep, ChatMessage, EssayRecord, ThinkingSnapshot } from '../types';
import { TopicInput } from './TopicInput';

export function WritingDesk() {
  const [step, setStep] = useState<WritingStep>('topic-input');
  const [topic, setTopic] = useState('');
  const [questionId, setQuestionId] = useState<string | null>(null);
  const [preWriteMessages, setPreWriteMessages] = useState<ChatMessage[]>([]);
  const [content, setContent] = useState('');
  const [postWriteMessages, setPostWriteMessages] = useState<ChatMessage[]>([]);

  const handleTopicConfirm = (t: string, qId: string | null) => {
    setTopic(t);
    setQuestionId(qId);
    setStep('pre-write');
  };

  if (step === 'topic-input') {
    return <TopicInput onConfirm={handleTopicConfirm} />;
  }

  return <div>待实现: {step}</div>;
}
```

- [ ] **Step 5: 验证题目选择流程**

Run: `npm run dev`
操作: 选择一道题 → 点击"开始写作" → 页面显示 "待实现: pre-write"


---

### Task 3: Prompt 模板与 AI 服务

**Files:**
- Create: `src/prompts/index.ts`
- Create: `src/services/aiService.ts`
- Create: `src/services/storageService.ts`

- [ ] **Step 1: 创建 prompt 模板 src/prompts/index.ts**

```typescript
// 角色层：全局系统 prompt
export const ROLE_PROMPT = `你是一名思辨写作教练，你的学生是高分段中学生。

你的核心理念：作文差的根不在文笔，在思考。你的任务是引导学生自己发现思维的盲区、看到更深的层次，而不是替他们想好答案。

你的行为约束：
1. 永远不直接打分——评分只在最终总结时作为参考
2. 永远不替学生写——不给整段范文让学生照抄。只在学生完全卡住时，给一小段示范性分析，紧接着追问"你同意吗？你会怎么改？"
3. 永远不评价人格——不说"你想得太浅了"，只追问具体论证："这个论据真的支持你的观点吗？中间有没有逻辑跳跃？"
4. 永远不预设标准立意——不暗示只有一个正确答案
5. 追问必须指向具体内容——"你第二段的这个说法……"，不泛泛而谈
6. 先确认学生的意思再回应——"我理解你的意思是X，对吗？"
7. 每次对话结束时，做一个不超过三句话的阶段性小结

你的自适应原则：
- 当学生内容有明显的逻辑漏洞或方向偏差时，给方向、给脚手架
- 当学生方向正确但深度不够时，追问、制造认知冲突
- 当学生思维质量已经很高时，挑战、放手让学生自己评价

你的语气：平等、尊重、不居高临下。像一位比你经验丰富的朋友在和你聊天，不是一位老师在批作业。`;

// 场景层：写前引导 prompt
export const PRE_WRITE_PROMPT = `[pre-write]
现在你正在和学生进行写前对话。学生已经选择了一道作文题，你需要帮他们在动笔前想清楚。

你的引导策略：
1. 先让学生用自己的话说说对题目的理解——不要直接告诉他们题目应该怎么理解
2. 帮他们发现题目中的核心概念或核心矛盾。"这道题的关键词是什么？有没有隐含的前提？"
3. 引导他们列出可能的立意方向。如果他们只想到一个角度，追问"还有别的可能吗？"
4. 帮他们判断哪些立意是"大家都会想到的"（平庸），哪些是"更有新意的"
5. 最后问他们："你觉得哪个角度最值得写？为什么？"

注意：
- 每次只问一个问题，不要连珠炮
- 不要替学生列出立意方向，引导他们自己发现
- 不需要走完所有步骤，当学生已经展现出清晰的思路时就自然过渡

对话控制在 3-6 轮之间。当学生已经有明确的立意和写作方向后，说"你已经有很清晰的方向了，开始写吧"，让学生进入写作阶段。

学生选择的题目是：`;

// 场景层：写后复盘 prompt
export const POST_WRITE_PROMPT = `[post-write]
现在你正在和学生进行写后复盘对话。学生刚刚提交了一篇完整作文。

你的复盘策略：
1. 先让学生自评："你自己觉得哪段最有说服力？哪段最没把握？"
2. 在学生自评的基础上，分享你的观察。注意：分享的是"我注意到……"，不是"这篇作文的问题是……"
3. 聚焦两到三个具体点，而不是面面俱到。挑最重要的说
4. 如果学生的论证有逻辑跳跃或视角盲区，用追问的方式让他们自己发现
5. 如果学生的立意有平庸之处，不要直接说"这个立意平庸"，而是问"你觉得还有没有更深一层的可能？"
6. 最后做一个三句话以内的总结，包含：一个肯定、一个可改进的方向、一个下次可以尝试的视角

作文题目是：`;
```

- [ ] **Step 2: 创建 AI 服务 src/services/aiService.ts**

```typescript
import type { ChatMessage } from '../types';
import { ROLE_PROMPT, PRE_WRITE_PROMPT, POST_WRITE_PROMPT } from '../prompts';

// 使用 OpenAI-compatible API
// 配置方式：在 .env.local 中设置 VITE_API_KEY 和 VITE_API_BASE_URL
const API_KEY = import.meta.env.VITE_API_KEY || '';
const API_BASE = import.meta.env.VITE_API_BASE_URL || 'https://api.openai.com/v1';
const MODEL = import.meta.env.VITE_MODEL || 'gpt-4o-mini';

interface ApiMessage {
  role: 'system' | 'user' | 'assistant';
  content: string;
}

async function callLLM(messages: ApiMessage[]): Promise<string> {
  if (!API_KEY) {
    // 无 API Key 时返回模拟回复，用于开发调试
    return '[模拟教练回复] 你说得有点意思，但换一个角度再想想？';
  }

  const response = await fetch(`${API_BASE}/chat/completions`, {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      Authorization: `Bearer ${API_KEY}`,
    },
    body: JSON.stringify({
      model: MODEL,
      messages,
      temperature: 0.7,
      max_tokens: 800,
    }),
  });

  if (!response.ok) {
    const err = await response.text();
    throw new Error(`AI API 调用失败: ${response.status} ${err}`);
  }

  const data = await response.json();
  return data.choices[0].message.content;
}

function toApiMessages(messages: ChatMessage[]): ApiMessage[] {
  return messages.map((m) => ({
    role: m.role === 'user' ? 'user' : 'assistant',
    content: m.content,
  }));
}

export async function sendPreWriteMessage(
  topic: string,
  history: ChatMessage[],
  userMessage: string
): Promise<string> {
  const systemPrompt = ROLE_PROMPT + '\n\n' + PRE_WRITE_PROMPT + topic;
  const apiMessages: ApiMessage[] = [
    { role: 'system', content: systemPrompt },
    ...toApiMessages(history),
    { role: 'user', content: userMessage },
  ];
  return callLLM(apiMessages);
}

export async function sendPostWriteMessage(
  topic: string,
  essayContent: string,
  history: ChatMessage[],
  userMessage: string
): Promise<string> {
  const systemPrompt =
    ROLE_PROMPT +
    '\n\n' +
    POST_WRITE_PROMPT +
    topic +
    '\n\n学生作文全文如下：\n' +
    essayContent;
  const apiMessages: ApiMessage[] = [
    { role: 'system', content: systemPrompt },
    ...toApiMessages(history),
    { role: 'user', content: userMessage },
  ];
  return callLLM(apiMessages);
}

export function generateId(): string {
  return Date.now().toString(36) + Math.random().toString(36).slice(2, 8);
}
```

- [ ] **Step 3: 创建存储服务 src/services/storageService.ts**

```typescript
import type { EssayRecord } from '../types';

const STORAGE_KEY = 'zuowen-training-essays';

export function saveEssay(essay: EssayRecord): void {
  const existing = loadEssays();
  existing.push(essay);
  localStorage.setItem(STORAGE_KEY, JSON.stringify(existing));
}

export function loadEssays(): EssayRecord[] {
  try {
    const raw = localStorage.getItem(STORAGE_KEY);
    return raw ? JSON.parse(raw) : [];
  } catch {
    return [];
  }
}

export function loadEssay(id: string): EssayRecord | undefined {
  return loadEssays().find((e) => e.id === id);
}

export function deleteEssay(id: string): void {
  const filtered = loadEssays().filter((e) => e.id !== id);
  localStorage.setItem(STORAGE_KEY, JSON.stringify(filtered));
}
```

- [ ] **Step 4: 创建 .env.local 供本地开发**

```bash
# 默认值已在 aiService.ts 中设置，此文件为可选配置
VITE_API_KEY=your-api-key-here
VITE_API_BASE_URL=https://api.openai.com/v1
VITE_MODEL=gpt-4o-mini
```


---

### Task 4: 聊天气泡组件

**Files:**
- Create: `src/components/ChatBubble.tsx`, `src/components/ChatBubble.css`

- [ ] **Step 1: 创建 ChatBubble 组件**

```typescript
import type { ChatMessage } from '../types';
import './ChatBubble.css';

interface ChatBubbleProps {
  message: ChatMessage;
}

export function ChatBubble({ message }: ChatBubbleProps) {
  const isUser = message.role === 'user';

  return (
    <div className={`chat-bubble ${isUser ? 'user' : 'coach'}`}>
      <div className="chat-avatar">{isUser ? '我' : '教练'}</div>
      <div className="chat-content">
        <p>{message.content}</p>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: 创建 ChatBubble.css**

```css
.chat-bubble {
  display: flex;
  gap: 10px;
  margin-bottom: 16px;
  align-items: flex-start;
}

.chat-bubble.user {
  flex-direction: row-reverse;
}

.chat-avatar {
  width: 32px;
  height: 32px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 12px;
  font-weight: 600;
  flex-shrink: 0;
}

.chat-bubble.user .chat-avatar {
  background: #1a1a1a;
  color: #fff;
}

.chat-bubble.coach .chat-avatar {
  background: #f0f0f0;
  color: #1a1a1a;
}

.chat-content {
  max-width: 80%;
  padding: 10px 14px;
  border-radius: 12px;
  font-size: 14px;
  line-height: 1.7;
}

.chat-bubble.user .chat-content {
  background: #1a1a1a;
  color: #fff;
}

.chat-bubble.coach .chat-content {
  background: #f0f0f0;
  color: #1a1a1a;
}
```


---

### Task 5: 写前对话阶段

**Files:**
- Create: `src/components/PreWriteChat.tsx`
- Modify: `src/components/WritingDesk.tsx`

- [ ] **Step 1: 创建 PreWriteChat 组件**

```typescript
import { useState, useRef, useEffect } from 'react';
import type { ChatMessage } from '../types';
import { sendPreWriteMessage, generateId } from '../services/aiService';
import { ChatBubble } from './ChatBubble';

interface PreWriteChatProps {
  topic: string;
  messages: ChatMessage[];
  onMessagesUpdate: (messages: ChatMessage[]) => void;
  onStartWriting: () => void;
}

export function PreWriteChat({
  topic,
  messages,
  onMessagesUpdate,
  onStartWriting,
}: PreWriteChatProps) {
  const [input, setInput] = useState('');
  const [loading, setLoading] = useState(false);
  const bottomRef = useRef<HTMLDivElement>(null);

  // 进入时如果没有消息，AI 先发第一句话
  useEffect(() => {
    if (messages.length === 0) {
      sendCoachMessage('读完这道题，你的第一感觉是什么？先别急着下笔，我们聊聊。');
    }
  }, []);

  useEffect(() => {
    bottomRef.current?.scrollIntoView({ behavior: 'smooth' });
  }, [messages]);

  const sendCoachMessage = async (text: string) => {
    const msg: ChatMessage = {
      id: generateId(),
      role: 'coach',
      content: text,
      timestamp: Date.now(),
    };
    onMessagesUpdate([...messages, msg]);
  };

  const handleSend = async () => {
    const trimmed = input.trim();
    if (!trimmed || loading) return;

    const userMsg: ChatMessage = {
      id: generateId(),
      role: 'user',
      content: trimmed,
      timestamp: Date.now(),
    };
    const newHistory = [...messages, userMsg];
    onMessagesUpdate(newHistory);
    setInput('');
    setLoading(true);

    try {
      const reply = await sendPreWriteMessage(topic, messages, trimmed);
      const coachMsg: ChatMessage = {
        id: generateId(),
        role: 'coach',
        content: reply,
        timestamp: Date.now(),
      };
      onMessagesUpdate([...newHistory, coachMsg]);

      // 如果对话已达一定轮数且学生方向明确，教练会自然引导结束
    } catch (err) {
      const errorMsg: ChatMessage = {
        id: generateId(),
        role: 'coach',
        content: '抱歉，出了点问题。请稍后再试。',
        timestamp: Date.now(),
      };
      onMessagesUpdate([...newHistory, errorMsg]);
    } finally {
      setLoading(false);
    }
  };

  const handleKeyDown = (e: React.KeyboardEvent) => {
    if (e.key === 'Enter' && !e.shiftKey) {
      e.preventDefault();
      handleSend();
    }
  };

  return (
    <div className="pre-write-chat">
      <div className="pre-write-topic-banner">
        <span className="topic-label">当前题目</span>
        <p>{topic}</p>
      </div>

      <div className="chat-messages">
        {messages.map((msg) => (
          <ChatBubble key={msg.id} message={msg} />
        ))}
        {loading && (
          <div className="chat-loading">教练正在思考...</div>
        )}
        <div ref={bottomRef} />
      </div>

      <div className="chat-input-area">
        <textarea
          value={input}
          onChange={(e) => setInput(e.target.value)}
          onKeyDown={handleKeyDown}
          placeholder="说说你的想法..."
          rows={2}
          disabled={loading}
        />
        <button onClick={handleSend} disabled={loading || !input.trim()}>
          发送
        </button>
      </div>

      {messages.length >= 4 && (
        <button className="start-writing-btn" onClick={onStartWriting}>
          我已经想清楚了，开始写作
        </button>
      )}
    </div>
  );
}
```

- [ ] **Step 2: 更新 WritingDesk 集成 PreWriteChat**

Replace the placeholder `return <div>待实现: {step}</div>;` with:

```typescript
  if (step === 'pre-write') {
    return (
      <PreWriteChat
        topic={topic}
        messages={preWriteMessages}
        onMessagesUpdate={setPreWriteMessages}
        onStartWriting={() => setStep('writing')}
      />
    );
  }
```

Remember to add the import: `import { PreWriteChat } from './PreWriteChat';`


---

### Task 6: 写作编辑器

**Files:**
- Create: `src/components/Editor.tsx`, `src/components/Editor.css`
- Modify: `src/components/WritingDesk.tsx`

- [ ] **Step 1: 创建 Editor 组件**

```typescript
import { useState, useRef } from 'react';
import type { ChatMessage } from '../types';
import { generateId } from '../services/aiService';
import './Editor.css';

interface EditorProps {
  content: string;
  onContentChange: (content: string) => void;
  onSubmit: () => void;
}

export function Editor({ content, onContentChange, onSubmit }: EditorProps) {
  const [wordCount, setWordCount] = useState(0);
  const [showHelpInput, setShowHelpInput] = useState(false);
  const [helpText, setHelpText] = useState('');
  const textareaRef = useRef<HTMLTextAreaElement>(null);

  const handleChange = (e: React.ChangeEvent<HTMLTextAreaElement>) => {
    const text = e.target.value;
    onContentChange(text);
    setWordCount(text.replace(/\s/g, '').length);
  };

  const handleAskCoach = () => {
    // MVP: 暂存帮助请求，在写后复盘时一并处理
    // 此处先记录到 console，完整实现留到阶段二
    console.log('问教练:', helpText);
    setHelpText('');
    setShowHelpInput(false);
  };

  return (
    <div className="editor">
      <div className="editor-toolbar">
        <span className="word-count">{wordCount} 字</span>
        {wordCount > 0 && wordCount < 700 && (
          <span className="word-warning">建议 800 字以上</span>
        )}
      </div>

      <textarea
        ref={textareaRef}
        className="editor-textarea"
        value={content}
        onChange={handleChange}
        placeholder="开始写你的文章..."
        autoFocus
      />

      {showHelpInput && (
        <div className="help-input-area">
          <input
            type="text"
            value={helpText}
            onChange={(e) => setHelpText(e.target.value)}
            placeholder="向教练提问..."
          />
          <button onClick={handleAskCoach}>发送</button>
        </div>
      )}

      <div className="editor-actions">
        <button
          className="ask-coach-btn"
          onClick={() => setShowHelpInput(!showHelpInput)}
        >
          问教练
        </button>
        <button
          className="submit-btn"
          onClick={onSubmit}
          disabled={wordCount < 100}
        >
          提交作文
        </button>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: 创建 Editor.css**

```css
.editor {
  display: flex;
  flex-direction: column;
  height: calc(100vh - 140px);
}

.editor-toolbar {
  display: flex;
  justify-content: space-between;
  padding: 8px 0;
  font-size: 13px;
  color: #999;
}

.word-warning {
  color: #e88;
}

.editor-textarea {
  flex: 1;
  width: 100%;
  padding: 16px;
  border: 1px solid #eee;
  border-radius: 8px;
  font-size: 15px;
  line-height: 1.8;
  font-family: inherit;
  resize: none;
  background: #fafafa;
}

.editor-textarea:focus {
  outline: none;
  border-color: #1a1a1a;
  background: #fff;
}

.help-input-area {
  display: flex;
  gap: 8px;
  padding: 8px 0;
}

.help-input-area input {
  flex: 1;
  padding: 8px;
  border: 1px solid #ddd;
  border-radius: 6px;
  font-size: 14px;
  font-family: inherit;
}

.help-input-area button {
  padding: 8px 16px;
  border: none;
  border-radius: 6px;
  background: #1a1a1a;
  color: #fff;
}

.editor-actions {
  display: flex;
  justify-content: space-between;
  padding: 12px 0;
  gap: 12px;
}

.ask-coach-btn {
  padding: 10px 20px;
  border: 1px solid #ddd;
  border-radius: 8px;
  background: #fff;
  font-size: 14px;
  cursor: pointer;
}

.submit-btn {
  flex: 1;
  padding: 10px;
  border: none;
  border-radius: 8px;
  background: #1a1a1a;
  color: #fff;
  font-size: 14px;
  cursor: pointer;
}

.submit-btn:disabled {
  background: #ccc;
  cursor: not-allowed;
}
```

- [ ] **Step 3: 更新 WritingDesk 集成 Editor**

Add to the step conditionals in WritingDesk.tsx:

```typescript
  if (step === 'writing') {
    return (
      <Editor
        content={content}
        onContentChange={setContent}
        onSubmit={() => setStep('post-write')}
      />
    );
  }
```


---

### Task 7: 写后复盘对话

**Files:**
- Create: `src/components/PostWriteReview.tsx`
- Modify: `src/components/WritingDesk.tsx`
- Modify: `src/services/storageService.ts` (used to save completed essay)

- [ ] **Step 1: 创建 PostWriteReview 组件**

```typescript
import { useState, useRef, useEffect } from 'react';
import type { ChatMessage, EssayRecord, ThinkingSnapshot } from '../types';
import { sendPostWriteMessage, generateId } from '../services/aiService';
import { saveEssay } from '../services/storageService';
import { ChatBubble } from './ChatBubble';

interface PostWriteReviewProps {
  topic: string;
  essayContent: string;
  questionId: string | null;
  preWriteMessages: ChatMessage[];
  messages: ChatMessage[];
  onMessagesUpdate: (messages: ChatMessage[]) => void;
  onComplete: (snapshot: ThinkingSnapshot) => void;
}

export function PostWriteReview({
  topic,
  essayContent,
  questionId,
  preWriteMessages,
  messages,
  onMessagesUpdate,
  onComplete,
}: PostWriteReviewProps) {
  const [input, setInput] = useState('');
  const [loading, setLoading] = useState(false);
  const bottomRef = useRef<HTMLDivElement>(null);

  useEffect(() => {
    if (messages.length === 0) {
      sendCoachMessage(
        '写完了！先不急着听我的看法——你自己觉得，哪段写得最有说服力？哪段最没把握？'
      );
    }
  }, []);

  useEffect(() => {
    bottomRef.current?.scrollIntoView({ behavior: 'smooth' });
  }, [messages]);

  const sendCoachMessage = async (text: string) => {
    const msg: ChatMessage = {
      id: generateId(),
      role: 'coach',
      content: text,
      timestamp: Date.now(),
    };
    onMessagesUpdate([...messages, msg]);
  };

  const handleSend = async () => {
    const trimmed = input.trim();
    if (!trimmed || loading) return;

    const userMsg: ChatMessage = {
      id: generateId(),
      role: 'user',
      content: trimmed,
      timestamp: Date.now(),
    };
    const newHistory = [...messages, userMsg];
    onMessagesUpdate(newHistory);
    setInput('');
    setLoading(true);

    try {
      const reply = await sendPostWriteMessage(
        topic,
        essayContent,
        messages,
        trimmed
      );
      const coachMsg: ChatMessage = {
        id: generateId(),
        role: 'coach',
        content: reply,
        timestamp: Date.now(),
      };
      onMessagesUpdate([...newHistory, coachMsg]);
    } catch (err) {
      const errorMsg: ChatMessage = {
        id: generateId(),
        role: 'coach',
        content: '抱歉，出了点问题。请稍后再试。',
        timestamp: Date.now(),
      };
      onMessagesUpdate([...newHistory, errorMsg]);
    } finally {
      setLoading(false);
    }
  };

  const handleKeyDown = (e: React.KeyboardEvent) => {
    if (e.key === 'Enter' && !e.shiftKey) {
      e.preventDefault();
      handleSend();
    }
  };

  const handleFinish = () => {
    const snapshot: ThinkingSnapshot = {
      originality: 3,
      reasoning: 3,
      perspective: 3,
      structure: 3,
      language: 3,
    };
    const record: EssayRecord = {
      id: generateId(),
      questionId,
      topic,
      content: essayContent,
      preWriteMessages,
      postWriteMessages: messages,
      snapshot,
      createdAt: Date.now(),
    };
    saveEssay(record);
    onComplete(snapshot);
  };

  return (
    <div className="post-write-review">
      <div className="review-header">
        <h3>复盘对话</h3>
      </div>

      <div className="chat-messages">
        {messages.map((msg) => (
          <ChatBubble key={msg.id} message={msg} />
        ))}
        {loading && (
          <div className="chat-loading">教练正在思考...</div>
        )}
        <div ref={bottomRef} />
      </div>

      <div className="chat-input-area">
        <textarea
          value={input}
          onChange={(e) => setInput(e.target.value)}
          onKeyDown={handleKeyDown}
          placeholder="说说你的想法..."
          rows={2}
          disabled={loading}
        />
        <button onClick={handleSend} disabled={loading || !input.trim()}>
          发送
        </button>
      </div>

      {messages.length >= 3 && (
        <button className="finish-btn" onClick={handleFinish}>
          完成本次训练
        </button>
      )}
    </div>
  );
}
```

- [ ] **Step 2: 更新 WritingDesk 集成 PostWriteReview**

Add the `handleComplete` function and the `post-write` step:

```typescript
  const handleComplete = (_snapshot: ThinkingSnapshot) => {
    // MVP: 保存已完成，回到初始状态
    setStep('topic-input');
    setTopic('');
    setQuestionId(null);
    setPreWriteMessages([]);
    setContent('');
    setPostWriteMessages([]);
  };

  if (step === 'post-write') {
    return (
      <PostWriteReview
        topic={topic}
        essayContent={content}
        questionId={questionId}
        preWriteMessages={preWriteMessages}
        messages={postWriteMessages}
        onMessagesUpdate={setPostWriteMessages}
        onComplete={handleComplete}
      />
    );
  }
```

- [ ] **Step 3: 创建 .gitignore**

```gitignore
node_modules
dist
.env
.env.local
*.local
```

- [ ] **Step 4: 完整流程验证**

Run: `npm run dev`
操作: 
1. 选题 → 开始写作
2. PreWriteChat 阶段：AI 应该发第一条消息，学生回复，进行 3-4 轮对话
3. 点击"开始写作" → 进入编辑器
4. 输入至少 100 字 → 提交
5. PostWriteReview 阶段：AI 提问让学生自评
6. 完成 → 回到题目选择页
7. 打开浏览器控制台 → Application → Local Storage，确认作文记录已保存


---

### Task 8: 模拟回复模式与开发体验

**Files:**
- Modify: `src/services/aiService.ts`

- [ ] **Step 1: 添加开发用模拟回复**

When no API key is configured, return simulated responses that feel realistic. Modify the `callLLM` function's mock branch:

```typescript
async function callLLM(messages: ApiMessage[]): Promise<string> {
  if (!API_KEY) {
    return getMockResponse(messages);
  }
  // ... existing real API code
}

const MOCK_RESPONSES: Record<string, string[]> = {
  preWrite: [
    '读完这道题，你的第一感觉是什么？先别急着下笔，我们聊聊。',
    '有意思。你觉得这道题的核心矛盾是什么？',
    '那换个角度想想——如果你不同意自己刚才说的，你会怎么反驳？',
    '这个思路不错。但还有更深一层的可能吗？',
    '你已经有很清晰的方向了，开始写吧。',
  ],
  postWrite: [
    '写完了！先不急着听我的看法——你自己觉得，哪段写得最有说服力？哪段最没把握？',
    '我注意到你的论证在第三段有一个跳跃：从"竞争带来进步"直接跳到了"所以竞争是好的"。中间是不是少了"进步等同于好"这个前提的检验？',
    '你的立意抓住了题目的关键概念，这点很好。下次可以试试从另一个主体的视角看同一个问题——比如这道题里，除了"个体"的视角，还有"社会结构"的视角。',
    '总结一下：你的论证链条在大多数段落是清晰的，但需要更注意推理步骤的完整性。下次可以试着在写完每个论据后问自己：这个论据直接支持我的观点吗？中间有跳跃吗？',
  ],
};

let mockIndex = 0;

function getMockResponse(messages: ApiMessage[]): string {
  // 通过 system prompt 中的类型标记区分场景
  const systemMsg = messages[0]?.content || '';
  const isPreWrite = systemMsg.includes('pre-write');
  const pool = isPreWrite ? MOCK_RESPONSES.preWrite : MOCK_RESPONSES.postWrite;
  const idx = mockIndex % pool.length;
  mockIndex++;
  return pool[idx];
}
```


---

### Task 9: 最终集成与验证

- [ ] **Step 1: 检查所有文件已创建**

```bash
find src/ -type f | sort
```
Expected:
```
src/App.css
src/App.tsx
src/components/ChatBubble.css
src/components/ChatBubble.tsx
src/components/Editor.css
src/components/Editor.tsx
src/components/PostWriteReview.tsx
src/components/PreWriteChat.tsx
src/components/TopicInput.css
src/components/TopicInput.tsx
src/components/WritingDesk.tsx
src/data/questions.json
src/main.tsx
src/prompts/index.ts
src/services/aiService.ts
src/services/storageService.ts
src/types/index.ts
src/vite-env.d.ts
```

- [ ] **Step 2: 确认 TypeScript 编译无错误**

```bash
npx tsc --noEmit
```
Expected: 无错误输出

- [ ] **Step 3: 确认应用启动正常**

```bash
npm run dev
```
Expected: 应用在 localhost:3000 启动，无控制台错误

- [ ] **Step 4: 端到端流程测试（无需 API Key，使用模拟回复）**

操作流程：
1. 打开应用 → 看到题目选择页
2. 选择第 1 题 → 点击"开始写作"
3. 写前对话：收到 AI 第一条消息，输入回复 3 轮
4. 点击"我已经想清楚了，开始写作"
5. 编辑器中输入 200+ 字内容
6. 点击"提交作文"
7. 写后复盘：收到 AI 第一条自评提问，回复 2 轮
8. 点击"完成本次训练" → 回到题目选择页
9. 检查 localStorage 中保存了作文记录

- [ ] **Step 5: 创建 .gitignore**

```gitignore
node_modules
dist
.env
.env.local
*.local
```

---

## 阶段一 MVP 完成标准

- [x] 学生能选题或自定题目
- [x] 写前 AI 对话引导审题（3-6 轮）
- [x] 独立写作编辑器（字数统计、问教练按钮）
- [x] 写后 AI 对话复盘（先自评再 AI 观察）
- [x] 作文记录保存到 localStorage
- [x] 完整流程可闭环

阶段一的全部代码可在不配置 API Key 的情况下，使用模拟回复运行和调试。
